# <この PR が何をしたかを1行で>

<!-- 執筆指示: 一つのPRの変更と実装を案内し、レビュアーや後任が差分の意味と保証範囲を判断できるようにする。
     差分を説明する資料であり、既存処理を実行順に追う資料ではない。固定5節を保ち、目次から実装の全文へ直接進めるようにする。
     注釈は、変わっていない行にも振り、推測した理由やレビュー意見を混ぜない。 -->

> 型: 実装解説 ／ 読み手: <レビュアー・後任・実装を引き継ぐ人> ／ PR: [#<番号>](<PRのURL>)

- **リポジトリ**: <リポジトリ名>
- **取り込み先**: `<取り込み先ブランチ>` ／ **変更元**: `<変更ブランチ>`
- **状態**: レビュー中 / マージ済み / **マージ禁止（理由）**

<注意喚起があればここに置く。本番稼働中のロジックを置き換える、リリース時期が決まるまでマージしない、など。無ければ節ごと省く>

<何を、どこに、なぜ入れたか。読み手がこの変更について判断できること。変更外の処理や実行順の解説をどこまで扱うか>

## 差分注釈の凡例

<!-- 書く: 新規・変更なし・コメントのみ・削除を読み分ける表記。文書の冒頭へ置く。書かない: レビュー重大度、実行順注釈の区分。 -->

<掲載したコードの各行がどの区分かを、この表で読む。実行順注釈の区分は置かない>

| 表記 | 意味 |
|---|---|
| ［新規］ | 旧実装に無かった行 |
| ［ロジック変更なし］ | 呼ぶメソッド・条件・引数とも旧実装と同一 |
| ［コメントのみ変更］ | コードは同一で、コメントの文言・配置だけが変わった |
| ［削除］ | 旧実装にあって、今は無い |

## 目次

<!-- 書く: この文書内の固定5節への導線。書かない: 原典ファイル一覧。 -->

[① 処理の流れ](#①-処理の流れ) ／ [② 何が変わったか](#②-何が変わったか) ／ [③ 実装の全文](#③-実装の全文) ／ [④ テスト設計](#④-テスト設計) ／ [⑤ 気になった点](#⑤-気になった点)

## ① 処理の流れ

<!-- 書く: 変更後の主要な処理を入口から出口まで。書かない: コード全文や枝の内部。 -->
<番号つきの主要な流れ>

1. **<段階の名前>** — <何をするか>
2. **<段階の名前>** — <何をするか>

<複数の入口が同じ骨格に揃うなら、それを明示する>

## ② 何が変わったか

<!-- 書く: 変更単位、区分、旧状態、理由。書かない: ③で載せる全文の重複やレビュー意見。 -->
<差分の意味>

### `<関数名 / ファイル名>` — <何が変わったか>

<!-- 書く: 1つの変更単位だけ。書かない: 複数責務をまとめた説明。 -->

- **区分**: 新規 / 置き換え / シグネチャ変更 / 廃止 / 呼び出し元の追従
- **パス**: [<相対パス>](<commit を固定した恒久リンク>)

**旧実装**:

```<言語>
<置き換えられた側のコード。無ければ「新規のため旧実装なし」>
```

**なぜ変えたか**: <その PR が述べている理由。述べられていなければ「PR 本文に言及なし」>

## ③ 実装の全文

<!-- 書く: 読ませる層として選んだコード、該当行の直前に差し込んだ差分注釈、省いた箇所の印、見出し直下の原典の恒久リンク。書かない: 注釈の無い抜粋、行番号、コードの後ろへまとめた注釈表。 -->

### `<ファイルパス>`（[原典](<commit を固定した恒久リンク>)）

<!-- 書く: ファイルの責務、読ませる層のコード、`▼［区分］理由` の形で該当行の直前に置いた注釈、省いた箇所の `…` と原典リンク。読ませる層の各ブロックに、変わっていない行への注釈を最低1つ置く。書かない: 根拠のない実装意図、行番号。 -->

**このファイルの責務**: <どこから呼ばれ、何を確定させて返すか。1〜3文>

```<言語>
▼［新規］<なぜこの行が要るか>
<コード行>
▼［ロジック変更なし］<旧実装と同一であること。理由>
<コード行>
```

<省いた箇所があれば `…` と原典リンクの印を置く>

## ④ テスト設計

<!-- 書く: テスト名、保証する挙動、前提、未テストの理由。設計と実行結果を区別し、実行済みなら対象commit・条件・成否を添える。
     書かない: テストコードの全文転載、未実行を成功とする記載。 -->

| テスト名 | 何を確かめるか | 前提 |
|---|---|---|

<テストが無いなら「テストなし」と書き、なぜ無いかを書く。空欄にしない>

<計測結果を変更の効果の根拠にする場合は、比較条件と確認できた範囲を本文に残す>

## ⑤ 気になった点

<!-- 書く: 変更のリスク・未確認事項を重大度つきで。書かない: 事実説明へ混ぜた好み。 -->
<レビュー観点>

### <一行タイトル>

<!-- 書く: 1つのリスク、起こりうる影響、根拠。書かない: 断定できない内容の断定。 -->

- **重大度**: 注記 / 要確認 / 要修正

<何が起きうるか。断定できないなら「未確認」とし、何を確認すれば取り込みや修正を判断できるかを書く>

<無ければ「なし」と書く>
