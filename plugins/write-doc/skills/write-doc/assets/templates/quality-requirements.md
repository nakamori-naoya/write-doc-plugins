# <対象>の非機能要件

<!-- 執筆指示:
     この型は、機能が成り立つ条件とは別に、サービスが守る性能、信頼性、安全性、運用上の性質を、設計と検証に使える形で渡す。
     書く前に、主メッセージを一文で決める。「最優先で守るのは二重に成立させてはいけない結果であり、応答時間と可用性は初期値を置いて検証する」のように、最優先の性質と、まだ決まっていない範囲を一文に入れる。一文で書けなければ素材が足りないので、足りないことを依頼元へ返す。
     以下の見出しは論点の案内であり、名前も順序もそのまま写さない。対象に関係しない区分を、埋めるためだけに置かない。開く手間の大きい節は、見出しに結論を入れる（例: 「## 予約の成立だけは遅らせても曖昧にしない」）。短い節はラベルのままでよく、全部の見出しを同じ形にそろえない。
     既存資料を更新するときは、その資料の見出し、語り口、ほかの資料から参照されているアンカーを優先し、このテンプレートの見出しへ置き換えない。
     「高速」「高可用」とだけ書くと検証できないので、どこで測るか、何を測るか、閾値、時間窓、母集団、確かめ方に分けて書く。値が合意されていなければ、仮説か未決だと書く。
     利用・負荷モデルから導いた性質は、入力に同じ文言が無くても落とさない。どんな負荷があり、放っておくとどんな失敗が起き、だから何を守るのかを、地の文で因果としてつなげて書く。
     製品名や構成要素は、変えられない制約か、障害時の振る舞いを説明するのに要るときだけ書く。これから選べる実現方式を、非機能要件として固定しない。 -->

<冒頭の段落。誰が何の判断にこの資料を使うか、最優先で守る性質とその理由、今回決まっていない範囲を言い切る>

## 性能

<!-- 操作ごとに、処理量、応答時間、非同期に遅らせてよい処理、平常時・ピーク・短時間の突発の違いを書く。
     三つ以上の操作を同じ項目で比べるなら表にする。二つ以下なら段落で書く。どの値にも単位、時間窓、母集団、根拠の状態を添える。目標が未決なら、未決であることと決め方を書く。 -->

<利用者が待つ経路と、滞留を許せる経路。設計の基準と、まだ決まっていない目標>

## 整合性と鮮度

<!-- 成功した直後に必ず見える結果と、遅れて反映してよい結果を対にして書く。順序、重複、欠けからの回復、許せる不整合も書く。
     強く守る結果と遅れてよい結果は対比でつながるので、箇条書きに分けず地の文で書く。業務規則をここに書き写さず、選べる内部実装も書かない。 -->

<誰が、どの結果を、いつまでに、どの程度の確率または保証で見られるか>

## 負荷保護

<!-- 利用者ごとの集中、一つの操作から増える処理、共有資源の独占と、過負荷のときにも守る結果を書く。制限値が決まっていなければ、決めるのに使う観測を書く。
     利用者を不正と決めつける書き方や、根拠の無い制限値は書かない。 -->

<どの負荷を分けるか抑えるか、上限を超えたときに何を拒否または遅延させるか>

## 信頼性

<!-- 失敗しても失わない事実、再試行したときの結果、障害ごとに続ける機能と縮退・拒否する機能、復旧後の扱いを書く。
     障害が三種類以上あるときは「障害 | 継続する | 縮退・拒否する」の表で比べる。二つ以下なら段落で書く。 -->

<障害時にも守る結果と、正常に見せてはいけない状態>

## データの保持

<!-- 通常の利用経路で見せる期間や件数、容量を見積もる期間、物理削除・匿名化・法的保全、技術的な状態の保持を書く。
     業務上の期限と容量計算の仮定は、同じ数字でも意味が違うので分けて書く。 -->

<何をどれだけ保持し、いつ通常の利用から外し、物理的な消去をどう扱うか>

## セキュリティとプライバシー

<!-- 誰を本人として扱うか、越権を防ぐ境界、外から見える面、資格情報と秘密情報、扱う個人情報とその保護条件を書く。
     該当しない論点は、該当しない理由が判断に要るときだけ書く。 -->

<守る対象、許される主体、越えてはいけない境界>

## 運用性と観測

<!-- 正常、遅延、失敗を見分けるための観測、知らせる条件、復旧の確かめ方、バックアップと復元の要求を書く。互いに因果の無い観測点を並べるときは、箇条書きでよい。
     監視製品の設定手順や当番の手順は書かない。 -->

<運用担当者が何を観測し、どの状態を異常と判断できる必要があるか>

## 移植性と開発環境

<!-- 合意済みの言語、プロトコル、実行環境と、開発者が手元で再現できる範囲を書く。該当しなければ節ごと置かない。 -->

<実装と開発環境で固定された制約>

## まだ決まっていないこと

<!-- 未決の品質目標、決める人、決めるのに使う測定や試験、影響する設計判断を書く。未決どうしに因果が無ければ箇条書きでよい。未決が無ければ節ごと置かない。 -->

- <未決事項。誰が何を観測して決めるか>

## 追跡情報

<!-- 後続の資料がIDで品質要求を参照するときだけ、この節を置く。本文はこの表を見なくても読めるように書く。
     機械検査が読む記法: 見出しは「追跡情報」のまま、列名と順序は下の表のまま変えない。IDは QR- / QR-OQ- で一意にする。根拠と状態には、QR- なら agreed_decision / hypothesis / open_question のどれか一つ、QR-OQ- なら open_question を書き、引く上流のIDも同じセルに書く。 -->

| ID | 守る性質 | 観測・検証 | 根拠と状態 |
|---|---|---|---|
| <QR-ID> | <本文の品質要求> | <観測点・指標・閾値・時間窓・母集団・検証方法> | <要求・負荷ID、agreed_decision / hypothesis / open_question> |
