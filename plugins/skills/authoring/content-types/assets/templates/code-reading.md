# <追う処理の名前> を実行順に読む

<!-- 執筆指示: 各節の「書く/書かない」を満たしてから、このコメントを削除する。 -->

> 型: コードリーディング ／ 読み手: <その処理を自分で追う人> ／ 対象: [<リポジトリ/参照点>](<恒久リンク>)

- **入口**: `<最初に呼ばれる関数>`（[<相対パス>](<原典リンク>)）
- **出口**: `<何を返して終わるか>`
- **固定した参照点**: `<コミットハッシュ / タグ>` — **行番号はこの時点のもの**
- **追わない範囲**: <深追いしないもの。ここを書かないと読み手が際限なく潜る>

（一言でいうと。この処理は結局何を確定させて返すのか。2〜3文）

## ① 何を追うか

<!-- 書く: 読む目的、入口、出口、固定参照点、除外範囲。書かない: PR差分の評価。 -->
<追跡範囲>

**この文書は差分の説明ではない。** 変更の可否を判断したいなら実装解説（PR）を読む。

## ② 幹の一覧

<!-- 書く: 入口から出口までの主要段階と各段階で確定する値。書かない: 補助関数の内部段階。 -->
<番号つきの主要な筋>

| # | 段階 | この段階で確定するもの | 場所 |
|---|---|---|---|
| 1 | [<段階の名前>](#stage-1) | <何が決まるか> | `<ファイル:行>` |
| 2 | [<段階の名前>](#stage-2) | <何が決まるか> | `<ファイル:行>` |

## ③ 幹を実行順に読む

<!-- 書く: 実行順に主要logic、入力・確定値・分岐・副作用・次、途中注釈。書かない: 枝の実装全文やfile順の紹介。 -->
<段階1つにつき1ブロック。**ファイル単位ではなく、主要な制御フローの段階単位**で並べる。
呼び先の実装を見なくても入出力を説明できるなら、ここには呼び出し行・戻り値の意味・`→ 枝` のリンクだけを残す。呼び先の詳細コードを幹の途中へ挟まない>

### <a id="stage-1"></a>段階1 — <段階の名前>

<!-- 書く: この段階が所有する主要logicと原典link。書かない: 呼び先内部の枝葉。 -->

- **入力**: <受け取る値と、その出どころ>
- **確定する値**: <この段階を抜けた時点で決まっているもの>
- **分岐条件**: <どの条件でどちらへ行くか。無ければ「分岐なし」>
- **副作用**: <DB・キャッシュ・ログなど外へ出るもの。無ければ「なし」>
- **次の呼び出し**: <次にどこへ渡すか>

<この段階自身の主要ロジックを掲載する。**既定で見える状態にする。** 行番号は原典に合わせる>

<注釈は**コードの前だけでなく、途中へ差し込む**。分岐・確定・副作用の各行の直前へ置く>

<省いた行があるなら、その位置に省略の印を置き、原典の該当行へのリンクを添える>

<別パッケージ・別層の実装も確認する価値があるなら、呼び出し行の注釈へ
`→ [枝: <呼び先の名前>](#branch-<関数名のケバブケース>)` を置く。短いコードがこの段階の主張を直接証明する場合だけ、残す理由を注釈して幹へ掲載してよい>

→ 次: <段階2 の名前>

### <a id="stage-2"></a>段階2 — <段階の名前>

<!-- 書く: 段階1と同じ5項目を、この段階のコードで。書かない: 前段階の重複。 -->
<以下同じ>

## ④ 枝を読む

<!-- 書く: 幹の理解に効く別package・別layerの実装を初出順に。書かない: 枝からさらに続く全call graph。 -->
<幹からリンクした呼び先の詳細を、**幹で初めて呼ばれた順**に並べる。
枝は「掲載する価値がある実行ロジックだが、幹を先に完走させるため場所を分けたもの」。
枝の中からさらに呼ぶ先は原則として省略し、行番号つきの原典リンクで打ち切る>

<分岐で複数実装へ分かれる場合、実装本体は原則すべて枝へ置く。既定経路を先にし、他方が酷似しているなら差分だけを示す>

### <a id="branch-呼び先のケバブケース"></a>枝 — <呼び先の名前>

<!-- 書く: 幹との契約、幹に影響する内部logic、打ち切り先。書かない: 幹と無関係な実装詳細。 -->

← [幹の段階N — <段階名>へ戻る](#stage-N)

- **幹との契約**: <何を受け取り、何を返すか>
- **幹に効く点**: <この内部実装のどこが、幹の分岐・確定値・副作用へ影響するか>
- **ここで打ち切る先**: <さらに呼ぶが追わないもの。無ければ「なし」>

<詳細コードと途中注釈。省略した箇所には印と原典リンクを置く>

## ⑤ 分岐と終端

<!-- 書く: 正常、早期return、errorの条件・場所・結果。書かない: 到達不能な仮定。 -->
<すべての終端>

| 条件 | どこで | 返るもの |
|---|---|---|
| <条件> | `<ファイル:行>` | <戻り値・エラー> |

## ⑥ 未確認・追っていないもの

<!-- 書く: 未読の呼び先、検証していない前提、理由不明箇所。書かない: 推測で埋めた結論。 -->
<読んでいない先、確かめていない前提、原典に説明が無かった箇所。
**「たぶんこうだろう」は書かない。「未確認」と書く**>

- <項目> — **未確認**（理由）

## ⑦ 付録

<!-- 書く: 型・interfaceなど実行順を持たない参照情報。書かない: 呼び先の実行logic。 -->
<型定義・インターフェースなど、実行順を持たない参照情報。**畳んでよいのはここだけ。**
呼び先の実行ロジックはここへ置かず、④の枝へ置く。
生成物・巨大な JSON・mock は、載せずに原典へのリンクで済ませる>
