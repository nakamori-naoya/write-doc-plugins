# 品質要求正本 — <対象>

<!-- 執筆指示: 品質を観測点・指標・閾値・時間窓・母集団・検証方法へ落とし、根拠のない数値を確定しない。
     冒頭は本文段落で始め、誰が何のために読み、どこから始まり、何が観測できたら完了かを文章で運ぶ。型・対象・日付・確認者の一覧や引用blockを冒頭に置かない。 -->

<!-- 機械検査が読む記法（system-designの検査scriptはこの記法だけを構文解析する。ここに1度だけ書く）:
     - H2見出しはこのtemplateの名前と順序を固定する。追加・改名・並べ替えをしない。冒頭はH2より前の本文段落。節の中の本文段落は自由に書ける。
     - IDは `<接頭辞>-<3桁以上の数字>`。仮説は `QR-HYP-<数字>`、未決は `QR-OQ-<数字>`（資料接頭辞は要求発見=REQ、負荷=WL、品質=QR、構成=ARC。上流の未決を継続する行は上流のIDをそのまま使う）。図ノードは `NODE-<英大文字・数字>`。
     - 根拠状態の語は `fact` / `agreed_decision` / `hypothesis` / `open_question` の機械値をそのまま書く（backtick可）。
     - 表は見出し行の列名と順序をtemplateどおりにする。1セルに複数IDを書くときは `、` で区切る。該当なしは列の指示語（なし / 未決 / 未作成 / 非該当）を書き、空欄にしない。
     - `### <ID>: <一文>` の小見出しで要素を定義し、その本文の項目は `<ラベル>: ` で行を始める。`## 品質要求` の分類は 応答時間 / 処理量 / 可用性 / 整合性 / 耐久性 / 復旧性 / 安全性 / プライバシー / 運用性 / 費用 の10語、閾値は `<演算子> <数値> <単位>`（演算子は < / <= / = / >= / >。例 `<= 300 ミリ秒`）または 未決（`open_question` の行だけ）。`## 品質区分の網羅` は10区分を各1行、判定は 指定済み / 未決 / 非該当。矛盾IDは `QCON-`、状態は open / resolved で、open の矛盾は `## 仮説と未決` の `open_question` 行の影響先から参照する。 -->

<冒頭の言い切り。最優先の品質は何で、誰がどの検証で確かめるか。合意済みの品質要求と仮説のままの値、後続を止める未決を区別して示す>

## 対象と入力根拠

<!-- 書く: REQ/WL/Journey/Domain ID、根拠状態、対象母集団。書かない: 入力成果物の再設計。 -->

| 入力ID | 根拠状態 | 対象 | 品質判断への影響 |
|---|---|---|---|
| <REQ/WL-ID> | <状態> | <対象> | <一文> |

## 品質要求

<!-- 書く: QR-ID、分類、観測点、指標、閾値、時間窓、母集団、検証方法。書かない: 「高速」「高可用」だけの表現。 -->

| 品質要求ID | 分類 | 観測点 | 指標 | 閾値 | 時間窓 | 対象母集団 | 検証方法 | 根拠状態 |
|---|---|---|---|---|---|---|---|---|
| <QR-ID> | <10区分のどれか> | <どこで測るか> | <測る値> | <演算子 数値 単位、または 未決> | <期間> | <対象> | <試験・観測> | agreed_decision / hypothesis / open_question |

## 品質区分の網羅

<!-- 書く: 10区分それぞれについて、品質要求を置いたか、未決か、この対象には当たらないか。非該当は理由を書く。
     書かない: 区分を埋めるためだけの根拠のない品質要求。 -->

| 区分 | 判定 | 理由 | 品質要求ID |
|---|---|---|---|
| 応答時間 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 処理量 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 可用性 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 整合性 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 耐久性 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 復旧性 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 安全性 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| プライバシー | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 運用性 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |
| 費用 | 指定済み / 未決 / 非該当 | <理由> | <QR-ID、または なし> |

## トレードオフと矛盾

<!-- 書く: 品質どうし、要求・負荷仮説との矛盾と判断者。書かない: クラウド製品による解決。 -->

| ID | 対立するID | 内容 | 判断者 | 状態 |
|---|---|---|---|---|
| <QCON-ID> | <品質・負荷・要求ID> | <両立しない条件> | <誰> | open / resolved |

## 仮説と未決

<!-- 書く: 未合意値をhypothesis/open_questionとして検証計画付きで書く。書かない: 根拠なしの確定値。 -->

| ID | 根拠状態 | 内容 | 検証計画 | 影響先 |
|---|---|---|---|---|
| <QR-HYP-ID / QR-OQ-ID> | hypothesis / open_question | <内容> | <観測・合意> | <QCON・ADR・図ノードID、初回は 未作成> |

## 追跡

<!-- 書く: QR-IDからREQ/WL/ADR/図ノードへの対応。書かない: 根拠のない対応。 -->

| 品質要求ID | 要求ID | 負荷ID | ADR・図ノードID |
|---|---|---|---|
| <品質要求ID> | <要求ID> | <負荷ID> | <ADR・図ノードIDまたは未作成> |

## この資料に書かないもの

<!-- 書く: プロバイダー・製品選定、実装、運用手順の検討先。書かない: それらの内容。 -->

| 書かないこと | 検討先 |
|---|---|
| <別責務> | <対応する正本> |
