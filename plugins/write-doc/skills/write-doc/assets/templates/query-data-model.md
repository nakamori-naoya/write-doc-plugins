# <題材>のクエリデータモデル

<!-- 執筆指示: この資料では、コマンドデータモデルが設計したデータから、業務知識のクエリが求める読み取りを実現できるかを確かめる。自分のテーブルは持たない。
     冒頭の段落は、誰がどの読み取りで何を知るために、どのデータを読むかの一文から始める。
     議論の主役はBDDの Before と取得結果で、どのデータをどう組み合わせ、どの順で返すかを具体的な行で示す。
     業務知識が決めた表示対象と並び順だけを確かめる。一回に返す件数のように、表示対象と並び順を変えない値は書かない。
     実現できない読み取りは、この資料で列やテーブルを足して補わず、コマンドデータモデルの欠けとして未決に書き、差し戻す。
     速さ、キャッシュ、投影、索引、読み取り元の切り替え、SQLは書かない。どれも物理設計が決める。 -->

<!-- 検査が読む目印: ほかの package の検査がこの型の資料から読むのは、ここに書いた目印だけである。見出しの文言は読まないので、見出しには節の結論を入れてよい。
     読むテーブルを、見出し行が `| 読むテーブル | 持ち主の資料 |` の表に1テーブル1行で置く。テーブル名は backtick で囲み、持ち主の資料はそのテーブルを定義したコマンドデータモデルへの相対 Markdown リンクにする。
     この資料には、分類の表（`| 系列 | 性質 | 論理テーブル | … |`）と erDiagram を置かない。
     業務知識のBDDとの対応は、見出し行が `| 業務知識のBDD | この資料のBDD |` の表に、業務知識のクエリのBDDを1行ずつ置く。この資料のBDDの欄は、`BDD-<番号>` を `、` で区切ったものか `対象外` にする。
     BDD は `### [BDD-<3桁以上の数字>] <返る結果>` の見出しで一件ずつ置く。gherkin のブロックに続けて、`**Before**` の下に `` **`<テーブル名>`** `` とその表を、`**取得結果**` の下に返る結果の表を一つだけ置く。
     Before の表の見出し行は、持ち主の erDiagram の列名をそのまま使い、読む列だけを置く。取得結果の表の列は返す項目で、名前は自由である。0件なら先頭の列に「（行なし）」と書き残りの列を空けた行を一つ置く。
     同じ形の行が多いときは、範囲の一行で書いてよい。範囲の行は、値の変わるセルを `<先頭の値>〜<末尾の値>` と書く。先頭、末尾、境界の行は範囲に含めず、値で書く。 -->

<冒頭の言い切り。誰がどの読み取りで何を知るか。読むデータの持ち主>

## 読むテーブル

<!-- 書く: クエリが読むテーブルと、その持ち主のコマンドデータモデルを表にする。表の下に、テーブルをどう組み合わせるかをクエリごとに一段落で書く。
     段落には、どのテーブルのどの行を選び、何で結び付け、どの列で並べ、何を返すかを業務知識のクエリの言葉で書く。 -->

| 読むテーブル | 持ち主の資料 |
|---|---|
| `<テーブル>` | [<持ち主の資料>](<相対パス>) |

### <業務知識のクエリ>

<選ぶ行の条件、結び付け方、並べる列、返す項目。全体を読めないときと一件だけ壊れているときの結果をどのデータで見分けるか>

## 業務知識のBDDとの対応

<!-- 書く: 業務知識のクエリのBDDを一行ずつ置く。セルには番号か短い語だけを書き、対象外にした理由は表の後の本文にまとめる。 -->

| 業務知識のBDD | この資料のBDD |
|---|---|
| BDD-001 | BDD-001 |

<対象外にしたBDDと、その理由>

## 未決

<!-- 書く: 確かめていない読み取りの条件と、それを誰に確かめるか。コマンドデータモデルに無いため実現できない読み取りは、どのクエリのどの項目に、どのデータが無いかを書き、コマンドデータモデルへ差し戻すと書く。
     一件ずつ段落で書く。無ければ「なし」と一文で書く。 -->

<未決／なし>

## BDD

<!-- この節は資料の末尾に置く。業務知識のクエリの掲載順に置く。
     各BDDは単独で読めるようにし、必要な事前状態、既存の行、日時をすべてGivenとAndに書く。Whenには読み取りを一つ置き、Thenに返る結果を書く。
     Before には、表示対象に入る行だけでなく、条件の境で外れる行（ほかの人の行、状態の違う行、期限の境の行）も置き、取得結果に現れないことで外れたと分かるようにする。
     取得結果の行は、返す順に並べる。年月日は「YYYY年M月D日」、時刻は「HH:MM」で書く。 -->

### [BDD-001] <返る結果>

```gherkin
Given: <読む人と事前状態>
  And: <既存の行と時刻>
When: <業務知識のクエリ>
Then: <返る結果と順序>
```

#### データの状態

**Before**

**`<テーブルA>`**

| <列> | <列> |
|---|---|
| <値> | <値> |
| <先頭の値>〜<末尾の値> | <同じ値> |

**取得結果**

| <返す項目> | <返す項目> |
|---|---|
| <値> | <値> |
