# write-doc-cleanup

最終成果物と入力資料を保持し、資料化のためだけに生成した中間ファイルを明示パスで削除する。

削除対象を拡張子やdirectory名から推測しない。repository内の未追跡ファイルだけを扱い、追跡中のファイル、directory、repository外、`.git`配下、保持対象と一致するパスは拒否する。

最終資料を `--keep`、資料作成後に不要になった未追跡の中間成果物を `--delete` として明示する。削除対象が明示されていない場合は何も削除しない。
