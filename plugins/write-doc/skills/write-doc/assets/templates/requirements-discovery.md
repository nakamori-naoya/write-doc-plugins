# <対象>の要件

<!-- 執筆指示:
     読み手が一読で「何を作り、何を満たせば完了か」を判断できる要求資料にする。
     既存の資料を更新するときは、その資料の見出し、語り口、他の資料からリンクされている見出し（anchor）を残し、このtemplateの見出しへ置き換えない。
     新しく書くときも、以下の節は扱う論点の案内として使う。節の名前、順序、粒度は素材と読後の判断に合わせて変え、書くことが無い節は置かない。
     根拠資料への参照は、それを使う主張のすぐ近くに置く。参照だけを集めた節は作らない。
     要求ID、根拠ID、根拠の確かさ、コマンドかクエリかの分類は、読み手が判断に使うときか、後続の資料が追跡に使うときだけ本文に出す。追跡に要る場合も、IDごとの定型見出しで本文を組まず、主張の近くでの参照か、追跡の表にまとめる。
     操作がデータを変えるか読むだけかを書いても、利用者の判断は変わらない。書くのは、操作の意味、成功したときの結果、失敗する条件、対になる操作が範囲を変えるときに限る。
     技術方式を残すのは、利用者が固定した制約か、合意済みの判断か、利用者から見える結果を変えるときだけで、それ以外は詳細設計へ送る。
     冒頭の段落では、誰が何を得られるか、今回の範囲、完了の見通しを言い切る。
     案件に固有の値、役割名、製品名を既定値として書かない。 -->

<!-- 検査が読む目印: ほかの package の検査がこの型の資料から読むのは、ここに書いた目印だけである。見出しの文言は読まないので、見出しには節の結論を入れてよい。
     接頭辞は `REQ` である。
     ID は `<接頭辞>-<3桁以上の数字>`、仮説は `<接頭辞>-HYP-<数字>`、未決は `<接頭辞>-OQ-<数字>` で書く。根拠の状態は `fact`、`agreed_decision`、`hypothesis`、`open_question` の値をそのまま書く。
     後続の資料が ID で指すものは、下の見出し行の表（追跡の表）に1行ずつ置く。どの見出しの下に置いてもよい。一つのセルに複数の ID を書くときは `、` で区切る。「根拠と状態」のセルには状態の値を一つと、根拠にした ID を書く。
     用語定義を参照する資料は、`用語定義: <絶対パス> 版: <整数>`（参照しないなら `用語定義: なし`）の行を一つ置き、続けて `推奨用語名: <名>、<名>` の行を置く。コマンドとクエリの操作を並べるときは、見出し行が `| 操作 | 種別 |` で始まる表に置き、種別を `コマンド` か `クエリ` にする。
     ほかの資料の上流として読むときは、表の1列目と、`### <ID>: ` で始まる見出しの ID を、その資料が定義した ID とみなす。
     追跡の表は、`| ID | 本文で扱う要求 | 根拠 |` の表に REQ-、DRV-、CON-、DEC- を置き、仮説と未決があれば `| ID | 状態 | 後続で決める論点 |` の表に REQ-HYP- と REQ-OQ- を置く。 -->

<誰が何をできるようになるか。今回扱う範囲と、何を満たせば完了か>

## 提供する結果

<!-- 書く: 利用者やAPIの利用者が得る結果を、目的や行為ごとに、利用者の言葉の見出しと本文で書く。成り立つための重要な条件、境界の値、拒否したときに状態が変わらないことは、それに当たる結果のすぐ近くに書く。
     書かない: 要求IDだけの見出し、内部の部品ごとの説明、業務規則の丸写し。 -->

### <利用者の言葉による結果>

<誰が何をでき、成功した後に何を確かめられるか。主な条件と例外>

## 成功した操作と失敗時の扱い

<!-- 書く: 成功した直後に利用者が確かめられること、遅れて反映されること、同じ要求の繰り返し、競合、一部の障害、回復のときに守る結果。書くことが少なければ、「提供する結果」の該当する箇所へまとめる。
     書かない: 製品に固有の再試行の方式、内部のテーブルやキューの状態、障害の場面の総当たり。 -->

<成功、拒否、同じ要求の繰り返し、同時の操作、遅延、回復のそれぞれで、利用者または運用担当者に何が見えるか>

## サービス特性から必要になる性質

<!-- 書く: 利用が一部に偏る、一つの操作から処理が膨らむ、データが積み上がるなど、放っておくと要求を壊すサービスの性質と、避けたい結果。数値が仮説なら、仮説だと書く。
     書かない: 負荷モデルの計算の全文、キューやキャッシュのような解決の方式、仮説を確定した要件のように書くこと。 -->

<どんな特性があり、そのせいで誰がどう困り、だから要件としてどんな性質が要るか。負荷、品質、構成の設計で後から決めること>

## 固定制約

<!-- 書く: 利用者が固定した技術、期間、提出物、環境の制約。
     書かない: 制約から導いた実装の方式、確かめていない制約、後続の設計で選べる製品。 -->

- <固定された制約>

## 対象範囲

<!-- 書く: 提供する価値、今回実装するもの、設計だけを示すもの、対象外を、読み手が取り違えない細かさで書く。既存の資料に合う区分があれば、その区分を使う。
     書かない: 将来の候補の詳細、業務知識やデータモデルの丸写し。 -->

| 区分 | 内容 |
|---|---|
| 提供する価値 | <利用者が得る結果> |
| 実装するもの | <今回作る範囲> |
| 設計として示すもの | <設計するが今回は構築しない範囲> |
| 対象外 | <今回扱わない範囲> |

## 完了の判断

<!-- 書く: 要求を満たしたと判断できる、外から確かめられる結果。利用者向けの機能、失敗と回復、設計の説明など、読み手が判断しやすい単位でまとめる。
     書かない: 内部の実装があることだけを理由にした完了、層ごとのテストケース、根拠の無い性能目標。 -->

- <誰がどこで何を確かめられれば完了か>

## 後続で決めること

<!-- 書く: 今回は決めない論点と、決めるために何を観測または確認するか、その間も守る成果。未決が無ければこの節を置かない。
     書かない: 未決の論点を合意済みの要件のように言い切ること、実装の詳細の先取り。 -->

- <未決の論点。何を根拠に、誰が決めるか>
