# 要求発見正本 — <対象>

<!-- 執筆指示: 観測事実・仮定 → 明示要件・固定制約 → 導出要件 → 設計判断 → 実装詳細 の5段階を判断境界にする。
     この資料は導出要件まで進み、設計判断は明示・不可避・合意済みのものだけを理由付きで残し、実装詳細は後続へ送る。
     節は因果の順に並べ、表で結論だけを並べず、本文の文で因果を一段ずつ読めるようにする。要求ID・根拠ID・根拠状態は残す。
     未決でも資料を保存し、後続へ渡せるかどうかだけを冒頭で閉じる。
     案件固有の値（件数、言語、通信方式、製品名）を既定の例として書かない。
     冒頭は本文段落で始め、誰が何のために読み、どこから始まり、何が観測できたら完了かを文章で運ぶ。型・対象・日付・確認者の一覧や引用blockを冒頭に置かない。 -->

<!-- 機械検査が読む記法（system-designの検査scriptはこの記法だけを構文解析する。ここに1度だけ書く）:
     - H2見出しはこのtemplateの名前と順序を固定する。追加・改名・並べ替えをしない。冒頭はH2より前の本文段落。節の中の本文段落は自由に書ける。
     - IDは `<接頭辞>-<3桁以上の数字>`。仮説は `REQ-HYP-<数字>`、未決は `REQ-OQ-<数字>`（資料接頭辞は要求発見=REQ、負荷=WL、品質=QR、構成=ARC。上流の未決を継続する行は上流のIDをそのまま使う）。図ノードは `NODE-<英大文字・数字>`。
     - 根拠状態の語は `fact` / `agreed_decision` / `hypothesis` / `open_question` の機械値をそのまま書く（backtick可）。
     - 表は見出し行の列名と順序をtemplateどおりにする。1セルに複数IDを書くときは `、` で区切る。該当なしは列の指示語（なし / 未決 / 未作成 / 非該当）を書き、空欄にしない。
     - `### <ID>: <一文>` の小見出しで要素を定義し、その本文の項目は `<ラベル>: ` で行を始める。`REQ-` は 受益者 / 必要な成果 / 検証方法 / 根拠 の4行、`DRV-` は 特性 / 失敗リスク / 必要な成果 / 設計への影響 / 見直し条件 の5行。`根拠:` と `特性:` には根拠ID（`SRC-`）と根拠状態を含める。本文で引く `SRC-` は `## 根拠` の表で定義したものだけを使う。
     - `## 用語` の1行目は `用語正本: <絶対path> 版: <整数>` または `用語正本: なし`。参照するときは2行目に `推奨用語名: <名>、<名>` を書く。 -->

<冒頭の言い切り。誰が何を合意するための正本か、どの観測と合意（根拠IDと根拠状態）から始まり、何が確定して何が未決なら後続へ渡せるか。未決が後続を止めるなら、その未決IDをここで示す>

## サービス特性と導出要件

<!-- 書く: 対象サービスの特性から、避けるべき失敗と必要な成果を導いた要件。導出要件（DRV-）ごとに小見出しを置き、
     「特性 → 失敗リスク → 避けるべき結果と必要な成果 → 設計への影響 → 見直し条件」を本文の文で一段ずつ書く。
     根拠にした観測事実・合意（根拠IDと fact / agreed_decision / hypothesis / open_question の根拠状態）と受益者を本文へ添える。
     利用者が明示した要求（REQ-）は、その受益者、何をなぜ必要とするか、検証方法、根拠を同じ形で書く。
     書かない: 表で結論だけを並べること、根拠の無い人物像、受入条件の体裁へ仮説を昇格すること。 -->

### <REQ-ID>: <要件を一文で>

<受益者: 誰がこの要件で何を得るか>
<必要な成果: 何をなぜ必要とするか。観測可能な結果>
<検証方法: 何を与えて何を観測すれば満たしたと言えるか>
<根拠: 根拠ID（SRC-）と根拠状態>

### <DRV-ID>: <要件を一文で>

<特性: どの観測・合意（根拠ID、根拠状態）から、対象サービスのどんな性質が分かるか>
<失敗リスク: その性質のもとで放置すると何が起き、誰が困るか>
<必要な成果: 避けるべき結果と、代わりに保証したい観測可能な結果。誰がどこで観測するか>
<設計への影響: この要件が後続のどの判断（負荷・品質・構成）を拘束するか>
<見直し条件: どの観測や決定が変わればこの要件を見直すか>

## 負荷・偏りの暫定分類

<!-- 書く: 導出要件から見える負荷の種類を、対象サービスの言葉で中立に分類する。操作が短時間に集中する種類と、一つの操作から処理が増幅する種類を分けて書く。
     数値は利用・負荷モデルへ委ね、ここでは分類と、その分類が要る理由だけを書く。
     書かない: 利用者を不正と決めつける呼称、キューやキャッシュなどの解決方式、単位の無い数値。 -->

| 分類 | 何が起きる負荷か | 根拠にした要件 | 負荷モデルで確かめること |
|---|---|---|---|
| <対象サービスの言葉で> | <一文> | <DRV / REQ ID> | <平均・ピーク・増幅のどれか> |

## 理由付き設計判断

<!-- 書く: 明示された、または論理的に不可避な、または合意済みの設計判断だけを「Xという特性がある。Yという失敗を避ける。だからZ」の形で書く。根拠状態を添える。
     書かない: 後続で決められる実装詳細、代替案の比較（それはADR）、合意していない判断の断定。 -->

### <DEC-ID>: <判断を一文で>

<特性（根拠ID）→ 避ける失敗 → だからこの判断。何を確定し、何を後続に残すか>

## 保持期間と期間上限後の定常状態

<!-- 書く: 何をどれだけの期間保持し、上限に達した後に通常利用経路からどう外れ、定常状態で何が残るか。根拠と根拠状態。
     書かない: テーブル設計、ストレージ製品、物理消去の手順。 -->

<対象ごとの保持期間と、上限後の定常状態。未決なら未決IDを示す>

## 固定制約

<!-- 書く: 利用者が固定した技術・期間・提出・環境の制約（CON-）と、それを固定した根拠。
     書かない: 制約から導いた設計判断（上の節）、未確認の制約の断定。 -->

| 制約ID | 制約 | 根拠ID・根拠状態 | 影響する要件・判断 |
|---|---|---|---|
| <CON-ID> | <一文> | <根拠> | <DRV / REQ / DEC ID> |

## スコープ

<!-- 書く: 提供価値・実装必須・設計説明のみ・対象外の4区分で閉じる。期間・基盤・再現範囲の予算を書く。
     書かない: Domain、論理データモデル、実装方式の内容。 -->

| 区分 | 内容 | 根拠ID・根拠状態 |
|---|---|---|
| 提供価値 | <利用者が得る結果> | <根拠> |
| 実装必須 | <今回作るもの> | <根拠> |
| 設計説明のみ | <設計として示すが実装しないもの> | <根拠> |
| 対象外 | <扱わないものと、その理由> | <根拠> |

<予算: 期間、基盤（利用できる環境・資源）、再現範囲（どこまで本番相当で確かめるか）>

## コマンドとクエリ

<!-- 書く: 状態を変える操作（コマンド）と読み取り専用の操作（クエリ）を分けた台帳。成功時に起きるコマンドイベント / クエリイベント、時間の経過で起きる時間イベント、システムが起こすシステムイベントを分ける。
     開始と解除、登録と取消のように対になる操作は、片方だけを載せず、対象内・対象外・未決のどれかを明示する。
     名前は用語正本の推奨用語名をそのまま使う。
     書かない: API名、画面操作、用語定義の複製。 -->

| 操作 | 種別 | 成功時に起きるイベント | イベント種別 | 対になる操作 | 対象 |
|---|---|---|---|---|---|
| <推奨用語名> | コマンド / クエリ / — | <イベント名または なし> | コマンド / クエリ / 時間 / システム / — | <同じ表の操作名または なし> | 対象内 / 対象外 / 設計説明のみ / 未決 |

## 用語

<!-- 書く: 用語正本の所在（path または URL）と版だけを示し、本文は用語正本の日本語の推奨用語名で書く。
     書かない: 定義本文の複製、同義語一覧、この資料だけで通用する別名。 -->

用語正本: <絶対path> 版: <整数>
推奨用語名: <名>、<名>

## 根拠

<!-- 書く: 本文で引用する観測事実・合意・仮説の出典と観測時点（SRC-）。根拠状態は fact / agreed_decision / hypothesis。
     書かない: 出典の要約だけで要件へ結び付かない記述、この表に無いSRC-の引用。 -->

| 根拠ID | 出典 | 観測時点 | 根拠状態 |
|---|---|---|---|
| <SRC-ID> | <資料・計測・決定・聞き取り> | <YYYY-MM-DD> | fact / agreed_decision / hypothesis |

## 後続設計で決める論点

<!-- 書く: この資料が決めず後続へ送る論点（routed_to）と、そこで守ってほしい成果。仮説（hypothesis）と未決（open_question）は検証計画と影響先を添える。
     書かない: 合意済みのような断定、論点の内容そのものの設計。 -->

| ID | 根拠状態 | 論点 | 送り先 | 守る成果 | 検証計画 |
|---|---|---|---|---|---|
| <REQ-HYP-ID / REQ-OQ-ID> | hypothesis / open_question | <問い> | <負荷・品質・データモデル・構成・実装のどれか> | <DRV / REQ ID> | <誰が何で確かめるか> |

## 観測可能な完了

<!-- 書く: 要求を満たしたと判断できる、利用者または運用上の観測可能な結果。誰がどこで何を観測すれば完了か。
     書かない: 実装方法、テスト手順、内部指標だけの完了条件。 -->

| 要件ID | 完了の観測 | 観測者・観測点 |
|---|---|---|
| <DRV / REQ ID> | <観測できる結果> | <誰が・どこで> |
