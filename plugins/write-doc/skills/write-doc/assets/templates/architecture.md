# システム構成 — <対象システム>

<!-- 執筆指示: いま何がどこで動き、何と通信し、どこに何が残り、どこが境界かを書く。守ることは次の三つである。
     - 書くのは今の構成と、その構成で狙っていることである。なぜこの構成が要り、全体がどう動くかは、各節の本文で説明する。構成の移り変わりや見送った案の比較はADRに任せ、将来の構成は「未決」に一行だけ置く。
     - 図、表、本文で同じ要素名を使う。図や表を並べるだけで終えず、主な流れと各要素の役割は本文で説明する。
     - 実行場所、名前、経路は、コンソールや設定と照らし合わせられる名前で書く。推定したものには「推定」と書く。
     サービス、ジョブ、保管場所のように動くものを構成要素と呼び、要素どうしの呼び出しやデータの受け渡しを通信と呼ぶ。今の構成が守っている条件は「制約と境界」に、決まっていないものは「未決」に置く。ADRが無ければ、「未決」に「決定の記録が無い」と書く。
     冒頭は本文の段落で始め、誰が何のために読み、構成のどこから読み始め、何を確かめられれば読み終えてよいかを文章で書く。 -->

<冒頭の言い切り。このシステムは何がどこで動き、どこが境界か。背景と、構成の中心にある狙い>

## 目的と範囲

<!-- 書く: 対象のシステム、含める環境、含めない隣接システムと、その資料の置き場。背景とシステムの前提は本文で短く書く。
     書かない: 判断に関係しない構成の移り変わりの全時系列。 -->

- 対象: <システム名と、含める範囲>
- 観測時点: <YYYY-MM-DD。構成をいつ確かめたか>
- 含めない: <隣接システムや外部サービスの内部。その資料の置き場>

## 全体構成

<!-- 書く: 読み手が境界と主な経路をつかむためのMermaidのflowchart。前後の本文で、リクエストがどの順に処理されるかという主な流れを説明する。
     書かない: 全部の属性、全部の通信、図だけで説明を終えること。 -->

```mermaid
flowchart LR
    利用者 --> 入口[<入口の要素>]
    入口 --> サービスA[<構成要素>]
    サービスA --> 保管[(<データの置き場>)]
```

## 構成要素

<!-- 書く: 変更や運用の判断に要る構成要素の、役割、実行場所、技術、所有者。表は要素が三つ以上あるときに使う。背景、選んだ理由、動き方を説明したい要素は、表のセルに詰めずに小見出しを立てて本文で書く。小見出しには要素名と、その要素について読み手が知るべきことを一言で入れ、全部を同じ形にそろえない。
     書かない: 内部の実装コード、判断に関係しない履歴。 -->

| 要素 | 役割（一文） | 実行場所 | 技術・製品 | 所有者 |
|---|---|---|---|---|
| <要素名> | <何を担うか> | <サービス名・リージョン・照合できる名前> | <ランタイム・製品・版> | <チーム・担当> |

## 通信と依存

<!-- 書く: 誰が誰を呼ぶか、経路（同期か非同期か）、認証、相手が応答しないときの振る舞い。主な処理の流れと、障害に耐える仕組みを先に本文で説明し、個々の通信は表で並べる。
     書かない: APIの項目定義、メッセージ形式の詳細。 -->

| 呼ぶ側 → 呼ばれる側 | 経路 | 認証 | 相手が応答しないとき |
|---|---|---|---|
| <要素> → <要素> | <同期HTTP／キュー／スケジュール> | <方式> | <再試行・失敗扱い・待ち> |

## データの置き場

<!-- 書く: どのデータがどこに残り、どれだけ保持し、どう復元するか。
     書かない: テーブル定義、スキーマ（コマンドデータモデルへ）。 -->

| データ | 置き場 | 保持 | 復元の手段 |
|---|---|---|---|
| <データの名前> | <保管場所の名前> | <期間・世代> | <バックアップ・再生成> |

## 環境

<!-- 書く: 環境ごとの構成の違いと、そこへの到達経路。環境が二つなら本文で本番との違いだけを書き、三つ以上なら表で比べる。違いが無い項目は「同じ」と書く。
     書かない: 環境の作り方、切り替えの手順。 -->

<本番の名前と到達経路。検証の名前と到達経路、本番と違う点。それ以外は同じであること>

## 制約と境界

<!-- 書く: 今の構成が守っている条件（処理量、可用性、セキュリティの境界、データの所在）と、それを変えるときに影響が及ぶ要素。
     書かない: 制約に至った経緯、将来の目標値。 -->

| 制約 | 値・条件 | 変えるときに影響する要素 |
|---|---|---|
| <処理量・可用性・境界> | <守っている値。推定なら「推定」> | <要素名> |

## 未決

<!-- 書く: 構成のうち確かめられていない点、決まっていない点と、何が分かれば確定するか。互いに独立した論点なので箇条書きにする。
     書かない: 決まったことの言い直し。未決が無ければ「なし」と書く。 -->

- <論点>。影響する要素: <要素名>。<何を確かめれば、または試せば確定するか>

## この資料に書かないもの

<!-- 書く: この資料では扱わない技術的な事項と、それを扱う資料。書かない: 書かない理由の長い説明。 -->

| 書かないこと | 検討先 |
|---|---|
| なぜこの構成にしたか | <ADR> |
| デプロイ・復旧・切り替えの手順 | <ハウツーガイド> |
| テーブルの形 | <コマンドデータモデル・RDB物理設計> |
| 業務の決まり | <業務知識> |
