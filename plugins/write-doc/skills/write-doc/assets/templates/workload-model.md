# 利用・負荷モデル — <対象>

<!-- 執筆指示: 調査報告ではなく設計入力モデルとして書く。採用する仮定を最初に示し、公開情報の説明から始めない。
     利用者（機械値: `actor`）・操作（機械値: `action`）・イベント（機械値: `event`）ごとに、平均だけでなく分布、ピーク、増加、偏りを残す。
     定量情報が無ければ推定し、定義・根拠種別・計算・時間窓・確度・再確認条件を持つ仮説として管理する。単位・時間窓・母集団・根拠が無い数値を確定しない。
     世界規模の参照値と初期実装の合格値を分ける。案件固有の値（件数、製品名）を既定の例として書かない。
     冒頭は本文段落で始め、誰が何のために読み、どこから始まり、何が観測できたら完了かを文章で運ぶ。型・対象・日付・確認者の一覧や引用blockを冒頭に置かない。 -->

<冒頭の言い切り。誰がどの容量・方式の判断に使うか、採用する仮定は何か、どの値が未決で最大容量の根拠に使えないか>

## 採用する仮定

<!-- 書く: 設計入力（DIN-）ごとに小見出しを置き、次の6項目を本文の文で閉じる。
     根拠（公開情報・実測・利用者決定・推定のどれか）／採用する仮定（何件・何人・何秒・何年として扱うか）／
     適用範囲（世界規模の参照値・初期実装の合格値・局所負荷のどれか）／要件への影響（補完するREQ / DRV / CON）／
     構成への影響（容量・分割・非同期化・流量制御・保持・削除のどれを判断するか）／見直し条件。
     書かない: 公開情報の解説から始めること、根拠種別を伏せた数値、単位や時間窓の無い値。 -->

### <DIN-ID>: <仮定を一文で>

<根拠: 公開情報 / 実測 / 利用者決定 / 推定 のどれか。出典と観測時点>
<採用する仮定: 何件・何人・何秒・何年として扱うか。単位と時間窓>
<適用範囲: 世界規模の参照値 / 初期実装の合格値 / 局所負荷 のどれか>
<要件への影響: 補完する REQ / DRV / CON のID>
<構成への影響: 容量・分割・非同期化・流量制御・保持・削除のどれを判断するか>
<見直し条件: どの観測や決定が変わればこの仮定を見直すか>

## 要件とインフラ判断への接続

<!-- 書く: 各仮定がどの要件を補完し、後続のどの判断（容量、分割、非同期化、流量制御、保持、削除）を拘束するか。書かない: 判断の結論そのもの（それはADR）。 -->

| 設計入力ID | 補完する要件 | 拘束する判断 | 根拠状態 |
|---|---|---|---|
| <DIN-ID> | <REQ / DRV / CON ID> | <容量・分割・非同期化・流量制御・保持・削除> | <fact / agreed_decision / hypothesis / open_question> |

## 初期実装で検証する境界

<!-- 書く: 初期実装の合格値として扱う値と、それを超えたときに見直す判断。世界規模の参照値と混ぜない。書かない: SLOとしての確定。 -->

| 設計入力ID | 初期実装の合格値 | 超えたときに見直す判断 | 検証方法 |
|---|---|---|---|
| <DIN-ID> | <値と単位、時間窓> | <判断> | <試験・観測> |

## 利用行為と規模

<!-- 書く: 利用者・操作・イベント、母集団、単位、時間窓、平均・ピーク率。書かない: 単位の無い数値やSLO。 -->

| 負荷ID | 利用者・操作・イベント | 母集団 | 平均率 | ピーク率 | 時間窓 | 根拠状態・根拠ID |
|---|---|---|---|---|---|---|
| <負荷ID> | <利用行為> | <対象件数> | <値と単位> | <値と単位> | <期間> | <状態・根拠ID> |

## 分布・偏り・バースト

<!-- 書く: 負荷の偏りを、操作頻度型（短時間に操作が集中する）と影響範囲型（一つの操作から処理が増幅する: fan-out・hot key）に分けて書く。
     分類名は対象サービスの言葉で中立に書く。書かない: 利用者を不正と決めつける呼称、キュー・シャード・キャッシュなどの解決方式。 -->

### 操作頻度型

| 負荷ID | 集中する操作 | 分布・偏り | バースト | 設計感度 |
|---|---|---|---|---|
| <負荷ID> | <操作> | <曜日・時間帯・地域・事業者等> | <倍率と継続時間> | <値が変わると影響する判断> |

### 影響範囲型

| 負荷ID | 増幅する操作 | fan-out | hot key | 設計感度 |
|---|---|---|---|---|
| <負荷ID> | <操作> | <1入力あたり件数> | <集中するキーとその割合> | <値が変わると影響する判断> |

## データ量・保持・増加

<!-- 書く: データ量（機械値: `payload`）、読み書き比、保持、増加率と計算。
     保持期間では、通常利用経路からの除外（一覧や検索に出なくなる）と物理消去（削除・アーカイブ・匿名化・法的保全・複製 / バックアップの扱い）を分けて書く。
     書かない: テーブルやストレージ製品。 -->

| 負荷ID | データ量 | 読み書き比 | 通常利用経路からの除外 | 物理消去 | 増加 | 計算根拠 |
|---|---|---|---|---|---|---|
| <負荷ID> | <値と単位> | <比率> | <期間と条件> | <削除・アーカイブ・匿名化・法的保全・複製の扱い> | <値・期間> | <式と根拠ID> |

## 計算

<!-- 書く: 各推定値の定義、根拠種別、式、時間窓、確度、再確認条件。書かない: 式の無い断定値。 -->

| ID | 定義 | 根拠種別 | 式 | 時間窓 | 確度 | 再確認条件 |
|---|---|---|---|---|---|---|
| <DIN / 負荷ID> | <何を数えるか> | 公開情報 / 実測 / 利用者決定 / 推定 | <式> | <期間> | <高・中・低とその理由> | <いつ何で確かめ直すか> |

## 調査根拠

<!-- 書く: 参照した公開情報・実測・利用者決定の出典と観測時点。書かない: 出典の要約だけで数値へ結び付かない記述。 -->

| 根拠ID | 出典 | 観測時点 | 使った設計入力 |
|---|---|---|---|
| <根拠ID> | <資料・計測・決定> | <YYYY-MM-DD> | <DIN-ID> |

## 見直し条件と未決

<!-- 書く: 仮説・未決、検証計画、影響するQR / ADR ID。モデル全体を見直す条件。書かない: 合意済み値への昇格。 -->

| ID | 根拠状態 | 内容 | 検証計画 | 影響先 |
|---|---|---|---|---|
| <識別子> | <仮説または未決> | <内容> | <観測・試験> | <品質要求・ADR・図ノードID> |

## この資料に書かないもの

<!-- 書く: SLO、データモデル、クラウド構成の検討先。書かない: それらの設計。 -->

| 書かないこと | 検討先 |
|---|---|
| <別責務> | <対応する正本> |
