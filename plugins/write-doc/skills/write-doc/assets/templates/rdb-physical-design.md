# RDB物理設計 — <題材>

<!-- 執筆指示: 検査済みの論理構造と、要求・利用負荷・品質要求・基盤制約を対象RDBとバージョンへ写す。
     論理設計のER図、CUDのBDD、Before / Afterは再掲しない。業務意味・関係・多重度・不変条件を変える必要が出たら、この資料で補わずRDB論理設計へ戻す。
     技術列、派生表、冗長化、materialized view、partitionは、論理意味を保ち、正本・同期・再構築・撤去方法を説明できる場合に物理写像として置ける。
     Readも設計対象にする。代表的な読み取りを資料の末尾へ置き、検索条件、結合、並び順、件数、鮮度、一貫性、SLO、実行計画からindexと配置を判断する。
     冒頭は本文段落で始め、誰が何のために読み、どこから始まり、何が観測できたら完了かを文章で運ぶ。型・対象・日付・確認者の一覧や引用blockを冒頭に置かない。 -->

<!-- 機械検査が読む記法（rdb-designの検査scriptはこの記法だけを構文解析する。ここに1度だけ書く）:
     - H2見出しはこのtemplateの11節（`## 対象と論理設計`〜`## 代表的な読み取り`）を名前どおり各1回置く。追加・改名をしない。
     - `## 対象と論理設計` の箇条書きは `- 対象DBMS:`、`- 対象バージョン:`、`- 論理モデル:`、`- 論理構造の指紋:`、`- 要求正本:`、`- 利用・負荷モデル:`、`- 品質要求正本:`、`- 基盤構成正本:`、`- 検証証拠:` の各行を1回置く。入力が無い場合も、非該当か未決かを理由つきで書く。
     - 要素は `### 物理写像: <名前>`、`### index: <名前>`、`### 分離性判断: <判断名>`、`### 機能: <機能名>`、`### 検証: <名前>`、`### Read-<連番>: <一文>` の小見出しで定義し、その本文の項目はtemplateの `- <ラベル>: ` 行を各1回、空にせず書く。機能名は重複しない。
     - index、分離性判断、機能の `- 検証状態:` と、検証の `- 状態:` は `verified` または `planned` とする。実機証拠が無い判断を `verified` にしない。
     - `### 機能:` の `- 根拠:` は対象版の公式資料のhttps URL、または `local:` で始まる実機確認の要約のどちらかで始める。
     - 論理設計の定義（`### テーブル:`、`#### 列:`、`#### 業務制約:`）と、BDD（`### Scenario`、行頭の `Given` / `When` / `Then` / `And`）をこの資料へ複製しない。論理設計の各業務制約の名前は、この資料の本文（`## 物理制約` の表）にそのまま現れる。 -->

<冒頭の言い切り。固定済みの論理構造を、どのRDBとバージョンでどう実現するか>

## 対象と論理設計

<!-- 書く: 対象DBMSの製品名・正確なバージョン、入力論理設計、検証環境、想定規模。書かない: 複数製品をまとめた曖昧な互換性。 -->

- 対象DBMS: <PostgreSQL／MySQLなど、製品名を一つ>
- 対象バージョン: <16.4／8.4.0など、正確なバージョン>
- 論理モデル: <入力ファイル名と更新日>
- 入力にした論理設計: <論理設計ファイルのpath>（版: <commit hash / 確定日など、後から同じ版を取り出せる識別子>）
- 論理構造の指紋: sha256:<検査toolが入力論理設計の論理テーブル・列・業務制約から計算したdigest>
- 要求正本: <絶対pathと版／非該当または未決の理由>
- 利用・負荷モデル: <絶対pathと版／未決の理由>
- 品質要求正本: <絶対pathと版／未決の理由>
- 基盤構成正本: <絶対pathと版／未決の理由>
- 検証証拠: <実行計画・負荷試験・競合試験・復旧試験・運用観測の絶対path／未実施の理由>
- 確認環境: <実機、公式資料、検証日時>
- 想定規模: <初期件数、増加率、保持期間>

<!-- 製品名とバージョンは必須。「Postgres系」「MySQL互換」「最新」のような幅を持つ指定にしない。
     型、index、制約、分離レベル、ロック、実行計画は、この製品・バージョンで利用できる
     機能だけを使う。別製品・別バージョンも対象なら、物理設計を分ける。 -->

## 物理制約

<!-- 書く: 各論理制約の対象DBMSでの実現、適用時点、違反時の業務結果。書かない: 論理設計に無い新しい業務規則。 -->

<!-- 論理制約を、主キー、一意制約、外部キー、CHECK、排他制約などへ写す。
     適用時点と失敗時の業務上の扱いまで書く。 -->

| 制約名 | 対象 | 実現方法 | 適用時点 | 違反時の扱い |
|---|---|---|---|---|
| <論理制約名> | <テーブル・列> | <対象RDBの機能> | <即時／遅延> | <中断、再試行、利用者へ返す結果> |

## 物理化の方針

<!-- 書く: 論理上の意味を対象DBMSの構造・型・制約・機能へ写す方針と理由。書かない: CREATE文やmigration全体。 -->

<!-- 技術列、派生表、冗長化、materialized view、partitionは物理写像として扱える。
     実行文はmigrationへ置き、ここには業務意味、不変条件、正本、同期、再構築、撤去の判断を残す。 -->

### 物理写像: <写像名>

- 論理上の意味: <入力論理設計で守る事実・関係・不変条件>
- 物理実装: <型、技術列、派生構造、冗長化、partitionなど>
- 正本と同期: <正本、更新契機、失敗時保証>
- 再構築・撤去: <再構築方法と不要時の戻し方>
- 不変条件の保存: <業務意味を変えない理由>

## index

<!-- 書く: 代表的なRead、制約、競合制御を支える根拠付きindex。書かない: 利用箇所と計測根拠の無い先回りindex。 -->

<!-- 既知の検索、結合、制約、競合制御へ必要なindexだけを書く。複合indexは列順の理由を、単一列indexは列を増やさない理由を書く。 -->

### index: `<index_name>`

<!-- 書く: 対象、種類、目的、列順、対象Read・更新、実行計画、更新費用。書かない: 名前だけのindex候補。 -->

- 対象: <テーブルと列・式>
- 種類: <B-tree／GiST、一意／複合／部分など>
- 目的: <検索／結合／制約／競合制御>
- 列の順番: <等価・範囲・並び順との対応。単一列なら増やさない理由>
- 対象Read・更新: <業務上のRead・更新>
- 根拠: <EXPLAIN、計測、対象バージョンの仕様>
- 更新費用: <書込み、容量、保守への影響>
- 検証状態: <verified／planned>

## トランザクションと分離レベル

<!-- 書く: 禁止結果から判断した分離レベル、制約・ロックとの組合せ、競合時の扱い。書かない: 解決したい現象と結びつかない分離レベル名。 -->

<!-- 解決策を先に選ばず、論理設計が許さない同時実行結果から必要な分離性を判断する。
     「存在しない予約枠」のように行ロックだけでは守れない対象を明記する。 -->

### 分離性判断: <判断名>

<!-- 書く: 一つの競合、禁止結果、起こり得る現象、選択、検証、業務結果。書かない: 別の競合を混ぜた一般論。 -->

- 同時に進む操作: <操作Aと操作B>
- 許してはいけない結果: <論理設計上の禁止結果>
- 発生し得る現象: <ロストアップデート／書き込みスキューなど>
- 選択する分離レベル: <READ COMMITTED／REPEATABLE READ／SERIALIZABLEなど>
- 併用する仕組み: <制約、明示的ロック、version検査。不要なら「なし」>
- 対象バージョンでの確認: <再現手順と結果>
- 競合時の扱い: <中断、再試行回数、利用者へ返す結果>
- 検証状態: <verified／planned>

#### 具体的な解決手順

<!-- 分離レベル名だけで済ませない。BEGINからCOMMITまでのSQL順序、検査する状態、
     制約・ロックが競合を検知する時点、DBエラーの識別、ROLLBACK、業務結果への変換、
     再試行の上限を対象RDBで実行できる粒度にする。 -->

1. <transactionを開始し、statement_timeoutとlock_timeoutを設定する。設定しないなら理由を書く>
2. <対象行を読み、事前状態を再検査する>
3. <現在値、占有、期限、イベントをどの順に書くか>
4. <どの制約違反・serialization failureを捕捉するか>
5. <commit、rollback、再試行、業務エラーへの変換をどう行うか>

```sql
BEGIN;
-- 対象RDBとバージョンで実行するSQL
COMMIT;
```

- 競合の検知: <SQLSTATE、更新件数0、version不一致など>
- 業務結果への変換: <利用者へ返す結果>
- 再試行: <対象エラー、最大回数、backoff、冪等性の識別子>
- 失敗時の原子性: <途中の記録が残らないこと>

## パーティションと配置

<!-- 書く: 採否、キー、配置、閾値、対象バージョンでの根拠。書かない: 件数と保守要件を伴わない流行上の選択。 -->

<!-- 採否、キー、保持期間、配置を容量・保守・対象バージョンから決める。不要なら根拠を書く。 -->

<採用内容、または採用しない理由>

## 容量・性能・運用

<!-- 書く: 件数、増加率、CUDとReadの頻度・SLO、統計、保守、RPO・RTO。書かない: 測定不能な「十分速い」。 -->

| 観点 | 前提・観測値 | 設計判断 | 確認方法・閾値 |
|---|---|---|---|
| データ量 | <件数・増加率> | <保持・分割・圧縮> | <確認方法> |
| 主要な書込み | <頻度・ピーク> | <競合・indexへの判断> | <確認方法> |
| 主要な読取り | <条件・SLO> | <index・統計への判断> | <確認方法> |
| バックアップ | <RPO・RTO> | <方式> | <復旧訓練> |

## 採用するRDB機能

<!-- 書く: 対象バージョンで採用する機能、採用箇所、検証結果、運用注意。書かない: 採用しない機能の一覧。 -->

### 機能: <機能名>

<!-- 書く: 一つの機能を選んだ理由、利用可能になった版、対象版の根拠、対象バージョンでの実証。書かない: 公式資料を読んだだけの未検証主張。 -->

- 採用箇所: <どの物理判断で使うか>
- 採用理由: <代替より適する理由>
- 利用可能な版: <この機能が使えるようになった版>
- 根拠: <対象版の公式資料のhttps URL、または local: で始まる実機確認の要約>
- 検証状態: <verified／planned>
- 対象バージョンで確認したこと: <確認方法と結果>
- 運用上の注意: <監視、migration、復旧への影響>

## 物理設計の完了条件

<!-- 書く: 対象バージョン、分離性の具体策、代表的Read、運用を検査できる条件。書かない: CUDのBDD本文。 -->

### 検証: <検証対象>

- 対象: <物理判断、品質要求、負荷条件、競合、障害、復旧>
- 状態: <verified／planned>
- 方法: <実行計画、benchmark、競合試験、障害試験、復旧試験、review>
- 合格条件: <観測可能な条件>
- 見直し条件: <負荷、品質、基盤、DBMS版の変化>
- 根拠: <証拠path、公式仕様、仮説の根拠>

## 未決

<!-- 書く: 未確認の機能、計測、容量、運用条件と確認方法。書かない: 決定済み事項の要約。 -->

<!-- 未確認の機能、計測、容量、運用条件と確認方法を書く。無ければ「なし」と明記する。 -->

<未決事項／なし>

## 代表的な読み取り

<!-- 書く: indexと配置を決める代表的なReadの検索形、件数、鮮度、一貫性、SLO、実行計画。書かない: 論理設計のCUDや画面名だけの要求。 -->

<!-- この節を資料の末尾に置く。画面名からindexを推測せず、業務で必要な読み取りを
     入力条件と結果の形へ落とす。同じ情報でも絞込み、並び順、件数、鮮度、一貫性が
     違えば別の読み取りとして扱う。対象バージョン・想定件数で実行計画と実測値を確認する。 -->

### Read-001: <誰が何を判断するための読み取りか>

<!-- 書く: 一つのReadの利用者、条件、結合、順序、上限、返却列、SLO、SQL、計測。書かない: SELECT *や未計測の高速性。 -->

- 利用者と目的: <業務上の役割と判断>
- 入力・検索条件: <等価条件、範囲条件、ページ位置>
- 結合: <テーブルと結合条件／なし>
- 並び順と上限: <安定した並び順、LIMIT>
- 返す情報: <必要な列。SELECT *にしない>
- 鮮度と一貫性: <primary必須／遅延許容、同一snapshotの必要性>
- 想定件数: <全体件数、対象件数、返却件数>
- SLO: <p95、p99、timeout>
- 支えるindex: `<index_name>`

```sql
SELECT <columns>
FROM <table>
WHERE <conditions>
ORDER BY <stable_order>
LIMIT <n>;
```

**対象バージョンでの確認**: <EXPLAIN (ANALYZE, BUFFERS)の主要node、推定行数と実行行数、実測値>
