# <この PR が何をしたかを1行で>

<!-- 執筆指示: 一つのPRで何が変わり、どう実装されたかを案内する。読み終えたレビュアーや後任が、差分の意味と、テストでどこまで確かめられているかを判断できるようにする。
     説明するのは差分である。変わっていない既存の処理を、実行順に追って解説する資料ではない。
     ①〜⑤の5節は、名前も順番も変えない。目次のリンク先になっているので、変えると目次から実装の全文へ飛べなくなる。
     差分注釈には、推測した理由やレビューの意見を混ぜない。変わっていない行にも注釈を振ってよいが、差分の意味を読むのに要る行に限る。
     冒頭は地の文の段落で始める。誰が何のために読むのか、どのPRを扱うのか、読み終えたら何を判断できるのかを、その段落で書く。 -->

<冒頭の段落。どのリポジトリで、どのbranchからどのbranchへ取り込むPRか（番号とURL）と、いまの状態（レビュー中、マージ済み、マージ禁止）を書く。マージ禁止なら、その理由もここに書く。本番で動いている処理を置き換える、リリース時期が決まるまでマージしない、といった注意も同じ段落に続ける>

<何を、どこに、なぜ入れたか。読み手がこの変更について何を判断できるか。変更していない処理や、実行順の解説をどこまで扱うか>

## 差分注釈の凡例

<!-- 書く: コードの行が、新規か、変更なしか、コメントだけの変更か、削除かを読み分けるための表記。文書の冒頭に置く。書かない: レビューの重大度。処理を実行順に追うときに使う注釈の区分。 -->

<掲載したコードの各行がどの区分かを、この表で読むことを一文で示す。処理を実行順に追うための区分は置かない>

| 表記 | 意味 |
|---|---|
| ［新規］ | 旧実装に無かった行 |
| ［ロジック変更なし］ | 呼ぶメソッド・条件・引数とも旧実装と同一 |
| ［コメントのみ変更］ | コードは同一で、コメントの文言・配置だけが変わった |
| ［削除］ | 旧実装にあって、今は無い |

## 目次

<!-- 書く: この文書の①〜⑤へ飛ぶリンク。書かない: 原典のファイル一覧。 -->

[① 処理の流れ](#①-処理の流れ) ／ [② 何が変わったか](#②-何が変わったか) ／ [③ 実装の全文](#③-実装の全文) ／ [④ テスト設計](#④-テスト設計) ／ [⑤ 気になった点](#⑤-気になった点)

## ① 処理の流れ

<!-- 書く: 変更後の主な処理を、入口から出口まで順に。書かない: コードの全文、分岐の先の細部。 -->
<番号つきの主な流れ>

1. **<段階の名前>** — <何をするか>
2. **<段階の名前>** — <何をするか>

<入口が複数あり、どれも同じ流れをたどるなら、そう書く>

## ② 何が変わったか

<!-- 書く: 変更のまとまりごとに、区分、変更前の状態、変えた理由。書かない: ③に載せる全文の繰り返し、レビューの意見。 -->
<差分の意味>

### `<関数名 / ファイル名>` — <何が変わったか>

<!-- 書く: 変更のまとまり1つだけ。見出しには、何が変わったかを書く。書かない: 役割の違う複数の変更をまとめた説明。 -->

- **区分**: 新規 / 置き換え / シグネチャ変更 / 廃止 / 呼び出し元の追従
- **パス**: [<相対パス>](<commit を固定した恒久リンク>)

**旧実装**:

```<言語>
<置き換えられた側のコード。無ければ「新規のため旧実装なし」>
```

**なぜ変えたか**: <その PR が述べている理由。述べられていなければ「PR 本文に言及なし」>

## ③ 実装の全文

<!-- 書く: 読み手に読ませると決めた範囲のコード、各行の直前に置いた差分注釈、省いた箇所の印、見出しのすぐ下に置く原典の恒久リンク。書かない: 注釈の無い抜粋、行番号、コードの後ろにまとめた注釈の表。 -->

### `<ファイルパス>`（[原典](<commit を固定した恒久リンク>)）

<!-- 書く: このファイルが何を受け持つか。読ませる範囲のコード。判断に要る行の直前に置く、`▼［区分］理由` の形の注釈。省いた箇所の `…` と原典リンク。変更していない行に注釈を置くのは、その行を読まないと差分の意味が分からないときだけにする。
     書かない: 根拠の無い実装の意図、行番号、数をそろえるためだけの注釈。 -->

**このファイルの責務**: <どこから呼ばれ、何を確定させて返すか。1〜3文>

```<言語>
▼［新規］<なぜこの行が要るか>
<コード行>
▼［ロジック変更なし］<旧実装と同一であること。理由>
<コード行>
```

<省いた箇所があれば `…` と原典リンクの印を置く>

## ④ テスト設計

<!-- 書く: テスト名、そのテストが確かめる振る舞い、前提、テストしていないものがあればその理由。テストの設計と実行結果は分けて書き、実行したなら、対象のcommit、条件、成否を添える。
     書かない: テストコードの全文、実行していないテストを成功と書くこと。 -->

| テスト名 | 何を確かめるか | 前提 |
|---|---|---|

<テストが無いなら「テストなし」と書き、なぜ無いかを書く。空欄にしない>

<測った結果を変更の効果の根拠にするなら、何と何を比べたかと、確かめられた範囲を本文に書く>

## ⑤ 気になった点

<!-- 書く: 変更に伴うリスクと未確認の点を、重大度を付けて。書かない: 事実の説明に紛れ込ませた好み。 -->
<レビュー観点>

### <一行タイトル>

<!-- 書く: リスク1つと、それで起きうること、そう考える根拠。見出しは、何が気になるのかが分かる一行にする。書かない: 断定できないことを断定する文。 -->

- **重大度**: 注記 / 要確認 / 要修正

<何が起きうるか。断定できないなら「未確認」とし、何を確認すれば取り込みや修正を判断できるかを書く>

<無ければ「なし」と書く>
