# RDB物理設計 — <題材>

<!-- 執筆指示: コマンドデータモデルのテーブルと業務制約を、一つのRDB製品の一つのバージョンへ写し、クエリデータモデルが確かめた読み取りを速く返す形を決める。冒頭の段落は、どの製品のどのバージョンで、データモデルの何を変えずに何を決めるかの一文から始める。
     データモデルのER図とBDDは写さない。業務の意味を変える必要が出たら、ここで補わずコマンドデータモデルへ戻す。 -->

<!-- 検査が読む目印: ほかの package の検査がこの型の資料から読むのは、ここに書いた目印だけである。見出しの文言は読まないので、見出しには節の結論を入れてよい。
     物理制約は、見出し行が `| 制約名 | 対象 | 実現方法 | 適用時点 | 違反時の扱い |` の表に1制約1行で置き、制約名の集合をコマンドデータモデルの `#### 業務制約: <名前>` の集合と一致させる。 -->

<!-- 冒頭の近くに、対象DBMS、対象バージョン、入力にしたコマンドデータモデルとクエリデータモデル、要求、利用負荷、品質要求、基盤構成の資料、検証証拠を一行ずつ置く。
     index は `| index | 対象 | 種類 | 支えるRead・更新 | 更新費用 | 検証状態 |`、代表的な読み取りは `| Read | 利用者 | 並び順と上限 | 鮮度と一貫性 | 想定件数 | SLO | 支えるindex |` の表に置き、支える Read と index は互いの表にある名前で書く。
     分離性の判断、採用するRDB機能、検証は、それぞれ小見出しを立て、検証の状態を verified か planned で書く。BDD は写さない。
     入力が無い入力根拠の行にも、非該当か未決かを理由とともに書く。 -->

<冒頭の言い切り。固定済みのデータモデルを、どのRDBのどのバージョンで、どう実現するか>

## 対象と入力

<!-- 製品名と正確なバージョンを一つだけ書く（「Postgres系」「最新」のような幅のある指定にしない）。 -->

- 対象DBMS: <PostgreSQL／MySQLなど、製品名を一つ>
- 対象バージョン: <16.4／8.4.0など、正確なバージョン>
- コマンドデータモデル: <資料のパス>（版: <コミットのハッシュや確定日など、後から同じ版を取り出せる識別子>）
- クエリデータモデル: <資料のパスと版／無い理由>
- 要求資料: <絶対パスと版／非該当か未決の理由>
- 利用・負荷モデル: <絶対パスと版／未決の理由>
- 品質要求資料: <絶対パスと版／未決の理由>
- 基盤構成資料: <絶対パスと版／未決の理由>
- 検証証拠: <実行計画、負荷試験、競合試験、復旧試験、運用観測の絶対パス／未実施の理由>
- 確認環境: <実機、公式資料、検証日時>
- 想定規模: <初期件数、増加率、保持期間>

## 物理制約

<!-- コマンドデータモデルの業務制約ごとに一行。表のすぐ後に、業務の表に置かないと決めた制約があれば、その理由を書く。 -->

| 制約名 | 対象 | 実現方法 | 適用時点 | 違反時の扱い |
|---|---|---|---|---|
| <業務制約の名前> | <テーブル・列> | <対象RDBの機能> | <即時／遅延> | <中断、再試行、利用者へ返す結果> |

## 物理化の方針

### 物理写像: <写像名>

<!-- 一つの写像について一、二段落で、物理でどう持つか、一次データと同期、作り直し方と撤去の仕方を書く。CREATE文は書かない。 -->

<データモデル上の意味、物理実装、一次データと同期、再構築と撤去、不変条件を保つ理由>

## index

<!-- 「対象」には、列や式を並べた順番どおりに書き、部分 index なら条件も書く。表の下に、index ごとに一段落で、目的、列の順番の理由、根拠を書く。 -->

| index | 対象 | 種類 | 支えるRead・更新 | 更新費用 | 検証状態 |
|---|---|---|---|---|---|
| `<index_name>` | `<table> (<列>, <列>)`<部分indexの条件> | <B-tree／GiST、一意／複合／部分など> | <Read-001、業務上の更新> | <書込み、容量、保守への影響> | <verified／planned> |

`<index_name>`は、<目的>。<列の順番の理由>。<根拠>。

## トランザクションと分離レベル

### 分離性判断: <判断名>

<!-- 一つの競合について一、二段落で、同時に何が進むと何が起きうるか、どの分離レベルと制約やロックで守るか、トランザクションの中で何回やり直すか、返す業務の結果、確かめ方を書く。 -->

<同時に進む操作と起きうる現象、選んだ分離レベルと仕組み、やり直しの回数、返す業務の結果、確かめ方>

検証状態: <verified／planned>

## パーティションと配置

<採用する内容、または採用しない理由と見直す閾値>

## 容量・性能・運用

| 観点 | 前提・観測値 | 設計判断 | 確認方法・閾値 |
|---|---|---|---|
| データ量 | <件数・増加率> | <保持・分割・圧縮> | <確認方法> |
| 主要な書込み | <頻度・ピーク> | <競合・indexへの判断> | <確認方法> |
| 主要な読取り | <条件・SLO> | <index・統計への判断> | <確認方法> |
| バックアップ | <RPO・RTO> | <方式> | <復旧訓練> |

## 採用するRDB機能

### 機能: <機能名>

<どの物理判断で使い、代わりの手段より何が合うか。対象バージョンで確かめた方法と結果、監視、マイグレーション、復旧への影響>

- 利用可能な版: <この機能が使えるようになった版>
- 根拠: <対象版の公式資料の https のURL、または local: で始まる実機確認の要約>
- 検証状態: <verified／planned>

## 物理設計の完了条件

### 検証: <検証対象>

<何を、どの方法で確かめ、何が観測できれば合格か。どの変化があれば見直すか。証拠のパスか、仮説の根拠>

- 状態: <verified／planned>

## 未決

<確かめていない機能、計測、容量、運用の条件と、その確かめ方>

## 代表的な読み取り

<!-- この節は資料の末尾に置く。クエリデータモデルが確かめた利用者の読み取りだけでなく、背景処理の走査、監視の集計、書込みの中の判定も載せる。表の下に、読み取りごとに一段落で、何を判断するためか、検索条件、結合、返す情報を書く。クエリデータモデルのBDDを実現する読み取りは、そのBDDの番号も書く。 -->

| Read | 利用者 | 並び順と上限 | 鮮度と一貫性 | 想定件数 | SLO | 支えるindex |
|---|---|---|---|---|---|---|
| Read-001 | <業務上の役割、背景処理、監視> | <安定した並び順、LIMIT／なし> | <プライマリ必須／遅延許容、同じスナップショットの必要性> | <全体件数、対象件数、返却件数> | <p95、p99、タイムアウト> | `<index_name>` |

Read-001は、<誰が何を判断するための読み取りか>。<入力と検索条件>。<結合する／結合しない>。<返す情報>。
