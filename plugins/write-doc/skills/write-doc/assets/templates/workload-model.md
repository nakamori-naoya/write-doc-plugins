# <対象>の利用規模と負荷モデル

<!-- 執筆指示:
     この型は、容量、分割、非同期化、流量制御、保持を決める人へ、どれだけの利用をどんな偏りで受けるかを、計算に使える値として渡す。
     書く前に、主メッセージを一文で決める。「平常月は検索と予約の1分ピークを設計入力にし、同じ枠への集中は規模を縮めても残る」のように、採用する値と、容量判断を支配する偏りを一文に入れる。一文で書けなければ素材が足りないので、足りない値を依頼元へ返す。
     以下の見出しは論点の案内であり、名前も順序もそのまま写さない。
     既存資料を更新するときは、その資料の見出し、語り口、ほかの資料から参照されているアンカーを優先し、このテンプレートの見出しへ置き換えない。
     平均だけで終わらせず、ピーク、短時間の突発、偏り、一つの操作から増える処理、データの累積まで書く。平均だけでは、同じ枠への集中や通知の増幅で起きる詰まりを見落とすからである。
     実測が無いときは推定してよい。そのかわり、値の定義、根拠の種類、式、時間窓、確からしさ、見直す条件を値の近くに書き、確定値のように扱わない。
     世界規模の参照値、今回の設計入力、初期実装で確かめる範囲は、同じ数字に見えても役割が違うので、混ぜずに書き分ける。 -->

<冒頭の段落。誰がどの判断にこのモデルを使うか、採用する規模とピーク、規模を縮めても残る偏りと増幅を言い切る。最大容量の根拠にできない未決があれば、ここで言う>

## 結論

<!-- 母集団、主な操作の設計基準、ピークと突発、非同期に滞留させてよい負荷、設計を左右する偏りを先に書く。調査の経緯から始めると結論が後ろへ回るので、経緯は次の節へ送る。 -->

<設計対象の規模と、容量判断を支配する負荷の性質>

## 採用値ができるまで

<!-- 公開情報、実測、利用者の決定、推定から、今回の値へどうつながったかを地の文で書く。つながりが何段にもなるときは図を使ってよい。今回の値に結び付かない出典は並べない。 -->

<参照値、推定、縮尺、今回の値の関係。規模を縮めても縮まない性質>

## 今回採用する利用量

<!-- 利用者、操作、イベントごとに、母集団、日次や月次の総量、算術平均、設計基準、ピーク、短時間の突発を書く。
     どの値にも単位と時間窓を付ける。一件の操作が複数の処理に増えるなら、その倍率を本文で説明する。 -->

<一人当たりの仮定から総量への計算、平均とピークの意味>

## 分布の偏りと処理の増幅

<!-- 曜日、時間帯、地域、利用者などへの集中と、一つの操作が複数の宛先へ広がる処理（fan-out）や、特定のキーへの集中（hot key）で増える量を書く。総量を縮めても同じ比率では縮まない性質は、そうだとはっきり書く。
     キュー、シャード、キャッシュのような解決方式は、この資料では決めない。 -->

<どこへ負荷が集中し、何倍に増え、どの設計判断に効くか>

## データ量・保持・増加

<!-- 一件の大きさ、読み書きの比、累積量、増え方、通常の利用経路から外す条件、物理削除・アーカイブ・匿名化・法的保全を書く。
     容量を見積もる期間は計算上の仮定であり、業務上の保持期限ではない。二つを同じ数字で書くときも、どちらの意味かを分けて書き、なぜその期間なのかも書く。 -->

<一定期間で累積する量と、その計算に含むもの・含まないもの>

## 設計へ渡す境界

<!-- 初期実装で受ける負荷、それを超えたら見直す判断、確かめ方を書く。サービスが守る目標値（SLO）そのものは品質要求の資料へ送る。 -->

| 対象 | 初期の設計入力 | 超えたときに見直す判断 | 検証方法 |
|---|---|---|---|
| <操作・イベント・データ> | <値、単位、時間窓> | <容量・分割・非同期化・流量制御・保持・削除> | <観測・試験> |

## 計算と仮定

<!-- 推定値の定義、根拠の種類、式、時間窓、確からしさ、見直す条件を書く。本文ですでに説明した式は、ここで繰り返さない。 -->

<採用した式、丸め方、確からしさ、見直す条件>

## まだ決まっていないこと

<!-- 最大容量を決められない理由になっている未決と、それを決める観測または合意、影響する品質や構成の判断を書く。未決が無ければ節ごと置かない。 -->

- <未決事項。どの観測または合意で決めるか>

## <後続の資料がIDで指す設計入力>

<!-- 後続の資料がIDで指す設計入力と未決を、write-doc の公開契約「検査が読む目印」の workload-model に従って、追跡の表に置く。本文はこの表を見なくても読めるように書く。 -->

<追跡の表>
