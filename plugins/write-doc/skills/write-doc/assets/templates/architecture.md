# システム構成 — <対象システム>

<!-- 執筆指示: いま、何がどこで動き、何と通信し、どこに何が残り、どこが境界かを宣言する。
     不変条件は3つ。
     1. 現在の構成と、その中核となる技術的意図を明確にする。長大な変遷や廃案の比較はADRへ委ねるが、なぜこの構成が必要で全体としてどう動くかという背景・メカニズム（Why/How）は、各節の本文段落で自然に解説する。将来の構成は「未決」に1行だけ置く。
     2. 図・表・本文が同じ要素名を使う。図や表の連打だけで終わらせず、主要な流れや各要素の役割・着眼点を本文のナラティブで解説する。
     3. 実行場所・名前・経路は、コンソールや設定で照合できる名前で書く。推定は「推定」と書く。
     判定: 動くもの（サービス・ジョブ・保管場所）は構成要素、要素どうしの呼び出し・データの受け渡しは通信。
     今の構成が守っている条件は制約、決まっていないものは未決。ADRが無いなら「未決」に「決定の記録が無い」と書く。
     冒頭は本文段落で始め、誰が何のために読み、どこから始まり、何が観測できたら完了かを文章で運ぶ。型・対象・日付・確認者の一覧や引用blockを冒頭に置かない。 -->

<冒頭の言い切り。このシステムは何がどこで動き、どこが境界か。背景と中核の狙い>

## 目的と範囲

<!-- 書く: 対象システム、含める環境、含めない隣接システムと、その資料の所在。背景やシステムの前提を本文で簡潔に語る。
     書かない: 決定に無関係な変遷の全時系列。 -->

- 対象: <システム名と、含める範囲>
- 観測時点: <YYYY-MM-DD。構成をどの時点で確かめたか>
- 含めない: <隣接システム・外部サービスの内部。その資料の所在>

## 全体構成

<!-- 書く: 読者が境界と主要経路を把握するためのMermaidのflowchart。前後の本文で、リクエストがどのような流れで処理されるか、主要な動線を解説する。
     書かない: 全属性、全通信、図だけで説明を済ませること。 -->

```mermaid
flowchart LR
    利用者 --> 入口[<入口の要素>]
    入口 --> サービスA[<構成要素>]
    サービスA --> 保管[(<データの置き場>)]
```

## 構成要素

<!-- 書く: 変更や運用の判断に必要な構成要素について、役割・実行場所・技術・所有者。表の前後に、各要素がシステム全体の中で果たす役割の要点や選定の技術的意図を本文で解説する。
     書かない: 内部の実装詳細コード、決定に無関係な履歴。 -->

| 要素 | 役割（一文） | 実行場所 | 技術・製品 | 所有者 |
|---|---|---|---|---|
| <要素名> | <何を担うか> | <サービス名・リージョン・照合できる名前> | <ランタイム・製品・版> | <チーム・担当> |

## 通信と依存

<!-- 書く: 誰が誰を呼ぶか、経路（同期・非同期）、認証、相手が落ちたときの振る舞い。主要な処理の流れと耐障害性のメカニズムを本文で語った上で、表で整理する。
     書かない: APIの項目定義、メッセージ形式の詳細。 -->

| 呼ぶ側 → 呼ばれる側 | 経路 | 認証 | 相手が応答しないとき |
|---|---|---|---|
| <要素> → <要素> | <同期HTTP／キュー／スケジュール> | <方式> | <再試行・失敗扱い・待ち> |

## データの置き場

<!-- 書く: どのデータがどこに残り、どれだけ保持し、どう復元するか。
     書かない: テーブル定義、スキーマ、論理データモデル（RDB論理設計へ）。 -->

| データ | 置き場 | 保持 | 復元の手段 |
|---|---|---|---|
| <データの名前> | <保管場所の名前> | <期間・世代> | <バックアップ・再生成> |

## 環境

<!-- 書く: 環境ごとの構成の差分と、到達経路。差分が無い項目は「同じ」と書く。
     書かない: 環境の作り方、切り替え手順。 -->

| 環境 | 本番との差分 | 到達経路 |
|---|---|---|
| <本番> | — | <URL・接続経路> |
| <検証> | <違う点だけ> | <URL・接続経路> |

## 制約と境界

<!-- 書く: 今の構成が守っている条件（処理量・可用性・セキュリティ境界・データの所在）と、
     変えるときに影響が及ぶ範囲。
     書かない: 制約に至った経緯、将来の目標値。 -->

| 制約 | 値・条件 | 変えるときに影響する要素 |
|---|---|---|
| <処理量・可用性・境界> | <守っている値。推定なら「推定」> | <要素名> |

## 未決

<!-- 書く: 構成のうち確認できていない点、決まっていない点と、何が分かれば確定するか。
     書かない: 決まったことの言い換え。0件なら「なし」と明記する。 -->

| 未決 | 影響する要素 | 何が分かれば確定するか |
|---|---|---|
| <論点> | <要素名> | <確認先・試すこと> |

## この資料に書かないもの

<!-- 書く: 技術事項と、その検討先。書かない: 書かない理由の長い説明。 -->

| 書かないこと | 検討先 |
|---|---|
| なぜこの構成にしたか | <ADR> |
| デプロイ・復旧・切り替えの手順 | <ハウツーガイド> |
| テーブル定義・論理データモデル | <RDB論理設計・RDB物理設計> |
| 業務の決まり | <業務知識・コアドメイン> |
