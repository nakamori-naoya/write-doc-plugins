# <対象>の要件

<!-- 執筆指示:
     この型の目的は、読み手が「何を作り、何を満たせば完了か」を一読で判断できる要求資料を作ることである。
     更新では既存資料の見出し、語り口、参照されているanchorを優先し、このtemplateの見出しへ置き換えない。
     新規作成でも、以下の節は論点の案内として使い、素材と読後ゴールに合う名前・順序・粒度へ変える。不要な節は置かない。
     要求ID、根拠ID、根拠状態、コマンド／クエリ分類は、読み手が判断に使う場合または後続資料との追跡に必要な場合だけ本文へ出す。
     根拠資料や詳細な参照元への参照は、判断に使う主張の近くへ置く。リンク集だけの節へ分離しない。
     追跡が必要でも、IDごとの定型見出しを本文の主構成にせず、主張の近くの参照か末尾の短い追跡表へ置く。
     状態変更か読み取りかという分類だけでは利用者の判断は変わらない。操作の意味、成功結果、失敗条件、対になる操作が範囲を変える場合に限って書く。
     技術方式は固定制約、合意済み判断、または利用者から観測できる結果を変える場合だけ残し、それ以外は詳細設計へ送る。
     冒頭は本文段落で始め、誰が何を得られるか、今回の範囲、完了の見通しを読み手の既知の語で言い切る。
     案件固有の値、役割名、製品名を既定値にしない。 -->

<誰が何をできるようになるか。今回扱う範囲と、何を満たせば完了か>

## 提供する結果

<!-- 書く: 利用者またはAPI利用者が得る結果を、目的や行為ごとの自然な見出しと本文で書く。重要な成立条件、境界値、拒否時に状態が変わらないことは該当する結果の近くに置く。
     書かない: 要求IDだけの見出し、内部コンポーネント別の説明、業務規則の網羅的な複製。 -->

### <利用者の言葉による結果>

<誰が何をでき、成功後に何を観測できるか。主要な条件と例外>

## 成功した操作と失敗時の扱い

<!-- 書く: 成功直後に利用者が確認できること、遅れて反映されること、再送・競合・部分障害・回復で守る結果。内容が少なければ「提供する結果」の該当箇所へ統合する。
     書かない: 製品固有の再試行方式、内部テーブルやキューの状態、障害シナリオの総当たり。 -->

<成功、拒否、再送、同時操作、遅延、回復について利用者または運用担当者が観測できる結果>

## サービス特性から必要になる性質

<!-- 書く: 利用頻度の偏り、一操作からの処理増幅、データ累積など、放置すると要求を壊す特性と、避けるべき結果。数値が仮説ならその状態を明示する。
     書かない: 負荷モデルの計算全文、キューやキャッシュなどの解決方式、仮説を確定要件として扱う表現。 -->

<特性 → 困る人と失敗 → 必要な性質。後続の負荷・品質・構成設計で決める内容>

## 固定制約

<!-- 書く: 利用者が固定した技術、期間、提出、環境の制約。短ければ箇条書き、対応関係の比較が必要なら表を使う。
     書かない: 制約から導いた実装方式、未確認の制約、後続で選べる製品。 -->

- <固定された制約>

## 対象範囲

<!-- 書く: 提供価値、今回実装するもの、設計として示すもの、対象外を、読み手が取り違えない粒度で示す。既存資料に適した区分があればそれを使う。
     書かない: 将来候補の詳細、業務知識やデータモデルの複製。 -->

| 区分 | 内容 |
|---|---|
| 提供する価値 | <利用者が得る結果> |
| 実装するもの | <今回作る範囲> |
| 設計として示すもの | <設計するが今回構築しない範囲> |
| 対象外 | <今回扱わない範囲> |

## 完了の判断

<!-- 書く: 要求を満たしたと判断できる観測可能な結果。利用者向け機能、失敗と回復、設計説明など、読み手が判断しやすい単位でまとめる。
     書かない: 内部実装の存在だけを根拠にした完了、層ごとのテストケース、根拠のない性能目標。 -->

- <誰がどこで何を観測できれば完了か>

## 後続で決めること

<!-- 書く: 今回は決めない論点、決めるための観測・確認、守るべき成果。未決が無ければこの節を置かない。
     書かない: 未決を合意済みの要件として断定すること、実装詳細の先取り。 -->

- <未決の論点。何を根拠に誰が決めるか>
