# RDB物理設計 — <題材>

<!-- 執筆指示: この資料では、検査済みの論理構造を、要求・利用負荷・品質要求・基盤の制約に合わせて、一つのRDB製品の一つのバージョンへ写す。
     書き始める前に、どの製品のどのバージョンで、論理設計の何を変えずに、何を決めるかを一文で決める。冒頭の段落はその一文から始め、太字で言い切る。

     論理設計のER図、データを変えるBDD、Before と After はこの資料に写さない。業務の意味、関係、多重度、不変条件を変える必要が出たら、この資料で補わずにRDB論理設計へ戻す。
     技術列、派生表、冗長化、materialized view、パーティションは、論理上の意味を保ったうえで物理写像として置ける。その場合は、一次データ、同期、再構築、撤去の方法を説明する。

     読み取りも設計の対象にする。代表的な読み取りを資料の末尾に置き、検索条件、結合、並び順、件数、鮮度、一貫性、SLO、実行計画から、index と配置を決める。 -->

<!-- 機械検査が読む記法（rdb-design の検査スクリプトは、この記法だけを構文解析する。記法はここにだけ書く）:
     H2見出しは、このテンプレートの11節（`## 対象と論理設計`〜`## 代表的な読み取り`）を名前どおり各1回置く。追加も改名もしない。
     `## 対象と論理設計` には、`- 対象DBMS:`、`- 対象バージョン:`、`- 論理モデル:`、`- 論理構造の指紋:`、`- 要求資料:`、`- 利用・負荷モデル:`、`- 品質要求資料:`、`- 基盤構成資料:`、`- 検証証拠:` の行を1回ずつ置く。入力が無いときも、非該当か未決かを理由とともに書く。
     `### 物理写像: <名前>`、`### 機能: <機能名>`、`### 検証: <名前>` には、テンプレートの `- <ラベル>: ` 行を1回ずつ、空にせずに書く。機能名は重複させない。`### 分離性判断: <判断名>` は、文章の最後の行に `検証状態:` を置く。
     `## index` と `## 代表的な読み取り` は、それぞれ最初の表をテンプレートと同じ列名・同じ並びで書き、セルを空にしない。index の名前は重複させない。Read の名前は `Read-` と3桁以上の数字にする。
     index の「支えるRead・更新」に書いた `Read-<番号>` は代表的な読み取りの表にあり、Read の「支えるindex」に書いた index は index の表にある。index が支えない Read には「なし」と書く。
     検証状態と、検証の `- 状態:` は `verified` か `planned` にする。`### 機能:` の `- 根拠:` は、対象版の公式資料の https のURLか、`local:` で始まる実機確認の要約で始める。
     BDD（`### Scenario`、行頭の `Given` / `When` / `Then` / `And`）をこの資料へ写さない。論理設計の各業務制約（`#### 業務制約:`）の名前は、`## 物理制約` の表にそのまま書く。 -->

<冒頭の言い切り。固定済みの論理構造を、どのRDBのどのバージョンで、どう実現するか>

## 対象と論理設計

<!-- 書く: 対象DBMSの製品名と正確なバージョン、入力にした論理設計、確認環境、想定規模。
     製品名とバージョンは必ず書き、「Postgres系」「MySQL互換」「最新」のような幅のある指定にしない。型、index、制約、分離レベル、ロック、実行計画は、この製品のこのバージョンで使える機能だけで決める。別の製品や別のバージョンも対象なら、物理設計を分ける。 -->

- 対象DBMS: <PostgreSQL／MySQLなど、製品名を一つ>
- 対象バージョン: <16.4／8.4.0など、正確なバージョン>
- 論理モデル: <入力ファイル名と更新日>
- 入力にした論理設計: <論理設計ファイルのパス>（版: <コミットのハッシュや確定日など、後から同じ版を取り出せる識別子>）
- 論理構造の指紋: sha256:<検査ツールが、入力にした論理設計の論理テーブル・列・業務制約から計算した値>
- 要求資料: <絶対パスと版／非該当か未決の理由>
- 利用・負荷モデル: <絶対パスと版／未決の理由>
- 品質要求資料: <絶対パスと版／未決の理由>
- 基盤構成資料: <絶対パスと版／未決の理由>
- 検証証拠: <実行計画、負荷試験、競合試験、復旧試験、運用観測の絶対パス／未実施の理由>
- 確認環境: <実機、公式資料、検証日時>
- 想定規模: <初期件数、増加率、保持期間>

## 物理制約

<!-- 書く: 論理設計の各業務制約を、主キー、一意制約、外部キー、CHECK、排他制約など対象DBMSのどの機能で守るか。適用される時点と、違反したときに業務としてどう扱うかも書く。
     表のすぐ後に、CHECK のように業務の表に置かないと決めた制約があれば、その理由を文章で書く。
     書かない: 論理設計に無い新しい業務規則。 -->

| 制約名 | 対象 | 実現方法 | 適用時点 | 違反時の扱い |
|---|---|---|---|---|
| <論理制約名> | <テーブル・列> | <対象RDBの機能> | <即時／遅延> | <中断、再試行、利用者へ返す結果> |

## 物理化の方針

<!-- 書く: 論理上の意味を、対象DBMSの構造、型、制約、機能へどう写すか。実行する文はマイグレーションに置き、ここには業務の意味、不変条件、一次データ、同期、再構築、撤去の判断を残す。
     書かない: CREATE文やマイグレーションの全体。 -->

### 物理写像: <写像名>

- 論理上の意味: <入力にした論理設計で守る事実・関係・不変条件>
- 物理実装: <型、技術列、派生構造、冗長化、パーティションなど>
- 一次データと同期: <一次データ、更新の契機、失敗したときの保証>
- 再構築・撤去: <作り直す方法と、要らなくなったときの戻し方>
- 不変条件の保存: <業務の意味を変えない理由>

## index

<!-- 書く: 代表的な読み取り、制約、競合の制御を支える index だけを一つの表にする。どの index がどの読み取りを支え、どれの更新費用が重いかを、行をまたいで比べられるようにするためである。
     「対象」には、テーブルと、列や式を並べた順番どおりに書く。部分 index なら条件も書く。
     表の下に、index ごとに一段落で、目的（検索、結合、制約、競合の制御）、列をその順番にした理由（単一列なら列を増やさない理由）、根拠（実行計画、計測、対象版の仕様、仮に置いた値）を書く。
     書かない: 使う箇所と根拠の無い先回りの index、表の列の言い直し。 -->

| index | 対象 | 種類 | 支えるRead・更新 | 更新費用 | 検証状態 |
|---|---|---|---|---|---|
| `<index_name>` | `<table> (<列>, <列>)`<部分indexの条件> | <B-tree／GiST、一意／複合／部分など> | <Read-001、業務上の更新> | <書込み、容量、保守への影響> | <verified／planned> |

`<index_name>`は、<目的>。<列の順番の理由>。<根拠>。

## トランザクションと分離レベル

<!-- 書く: 分離レベルと、トランザクションの中で繰り返す失敗の種類と回数を、操作ごとに一つに決める。実装はここで決めた指定を渡すだけにする。
     解決策を先に選ばない。論理設計の「並行実行で必要な保証」が許さない結果から、必要な分離性を決める。
     まだ無い行や件数の上限のように、行ロックだけでは守れない対象は、そうと分かるように書く。 -->

### 分離性判断: <判断名>

<!-- 書く: 一つの競合について一、二段落で、次を因果が読める順に書く。同時に何が進むと何が起きうるか、どの分離レベルと制約やロックで守るか、トランザクションの中でどの失敗を何回までやり直すか、利用者へ返す業務の結果、対象版でどう確かめるか。
     ここで決めるやり直しは、トランザクションの中の繰り返しである。回数を使い切った失敗を、処理の回収のやり直しで通すかどうかは、実装のエラーの分類が決める。
     最後の行に「検証状態: verified」か「検証状態: planned」を置く。
     書かない: 別の競合を混ぜた一般論、判断と結びつかない分離レベルの名前、「- 項目: 値」の箇条書き。 -->

<同時に進む操作と起きうる現象、選んだ分離レベルと仕組み、やり直しの回数、返す業務の結果、確かめ方>

検証状態: <verified／planned>

## パーティションと配置

<!-- 書く: 採るか採らないか、採るならキー、保持期間、配置を、件数、保守、対象バージョンから決めた理由とともに書く。採らないなら、見直す閾値を書く。
     書かない: 件数と保守の要件を伴わない、流行に合わせた選択。 -->

<採用する内容、または採用しない理由と見直す閾値>

## 容量・性能・運用

<!-- 書く: 件数、増加率、データを変える操作と読み取りの頻度とSLO、統計、保守、RPOとRTO。観点ごとに行を分け、前提、判断、確かめ方を行をまたいで比べられるようにする。
     書かない: 「十分速い」のように測れない言い方。 -->

| 観点 | 前提・観測値 | 設計判断 | 確認方法・閾値 |
|---|---|---|---|
| データ量 | <件数・増加率> | <保持・分割・圧縮> | <確認方法> |
| 主要な書込み | <頻度・ピーク> | <競合・indexへの判断> | <確認方法> |
| 主要な読取り | <条件・SLO> | <index・統計への判断> | <確認方法> |
| バックアップ | <RPO・RTO> | <方式> | <復旧訓練> |

## 採用するRDB機能

<!-- 書く: 対象バージョンで採用する機能を一つずつ、採用する箇所、使えるようになった版、根拠、確かめた結果、運用の注意とともに書く。
     書かない: 採用しない機能の一覧、公式資料を読んだだけで確かめていない主張。 -->

### 機能: <機能名>

- 採用箇所: <どの物理判断で使うか>
- 採用理由: <代わりの手段より合う理由>
- 利用可能な版: <この機能が使えるようになった版>
- 根拠: <対象版の公式資料の https のURL、または local: で始まる実機確認の要約>
- 検証状態: <verified／planned>
- 対象バージョンで確認したこと: <確かめた方法と結果>
- 運用上の注意: <監視、マイグレーション、復旧への影響>

## 物理設計の完了条件

<!-- 書く: 対象バージョン、分離性の具体策、代表的な読み取り、運用を、検査できる条件として書く。
     書かない: データを変えるBDDの本文。 -->

### 検証: <検証対象>

- 対象: <物理判断、品質要求、負荷条件、競合、障害、復旧>
- 状態: <verified／planned>
- 方法: <実行計画、ベンチマーク、競合試験、障害試験、復旧試験、レビュー>
- 合格条件: <観測できる条件>
- 見直し条件: <負荷、品質、基盤、DBMSのバージョンの変化>
- 根拠: <証拠のパス、公式仕様、仮説の根拠>

## 未決

<!-- 書く: 確かめていない機能、計測、容量、運用の条件と、その確かめ方。一件ずつ、互いに独立した項目として並べる。無ければ「なし」と書く。
     書かない: 決定済みの事項の要約。 -->

<未決事項／なし>

## 代表的な読み取り

<!-- この節は資料の末尾に置く。画面の名前から index を推測せず、業務に要る読み取りを、入力の条件と結果の形に落とす。
     同じ情報でも、絞り込み、並び順、件数、鮮度、一貫性が違えば、別の読み取りとして扱う。
     利用者の問い合わせだけでなく、背景処理の回収と走査、監視の集計、条件付きの書込みの判定（件数や不存在の確認）も載せる。利用者からは見えなくても、件数とともに遅くなる読み取りだからである。
     書く: 読み取りを一つの表にし、件数、SLO、支える index を行をまたいで比べられるようにする。表の下に、読み取りごとに一段落で、何を判断するための読み取りか、入力と検索条件、結合、返す情報（SELECT * にしない）を書く。対象版で確かめたら、実行計画の主なノード、推定行数と実行行数、実測値も書く。
     書かない: データを変える操作の本文、画面の名前だけの要求、計測していない速さの主張。 -->

| Read | 利用者 | 並び順と上限 | 鮮度と一貫性 | 想定件数 | SLO | 支えるindex |
|---|---|---|---|---|---|---|
| Read-001 | <業務上の役割、背景処理、監視> | <安定した並び順、LIMIT／なし> | <プライマリ必須／遅延許容、同じスナップショットの必要性> | <全体件数、対象件数、返却件数> | <p95、p99、タイムアウト> | `<index_name>` |

Read-001は、<誰が何を判断するための読み取りか>。<入力と検索条件>。<結合する／結合しない>。<返す情報>。
